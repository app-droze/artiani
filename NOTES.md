# NOTES
- 2026-03-26: Blocked auction-only products from the normal store purchase flow by hiding catalogue quick-add for auction items and rejecting them server-side in cart validation and order pricing. Why: the lamb-easter auction painting was still purchasable through the normal cart/checkout path, which is a real launch blocker for an auction-only item. Next: keep auction status as the single source of truth and later align catalogue card pricing with the live highest bid if the auction stays public on listing pages.
- 2026-03-26: Increased the desktop PDP magnifier lens size and kept the zoomed image content hidden until the large lens image has actually loaded. Why: the previous desktop lens felt too small for painting detail, and the magnifier could briefly show half-loaded content before the zoom asset was ready. Next: if the zoom still feels tight in use, tune lens diameter and zoom strength together rather than only enlarging one of them.
- 2026-03-26: Added a read-only auction monitoring section to `/admin/dashboard`, showing each auction’s product, status, timing, starting bid, current highest bid, bid count, minimum increment, and latest bid activity via protected server-side admin queries. Why: the dashboard already gated admin access, so the smallest useful next step was making auction health visible without creating a separate admin auction area or adding edit controls. Next: if auction operations expand, the next admin step should be a dedicated `/admin/auctions` page or a simple winner/finalization control set.
- 2026-03-26: Added a protected `/admin/orders/[orderCode]` detail page and linked each admin order row to it with a `View` action, showing stored order/customer fields, derived subtotal/shipping, and order-item snapshot data without adding edit actions or refactoring the admin area. Why: the orders list is useful for scanning and status updates, but real admin work also needs a simple drill-down into one order’s customer info and stored line-item snapshots. Next: if this page proves useful, the next admin improvement should be linking directly to it after status updates or adding a tiny timeline of future order events.
- 2026-03-26: Added auction bid confirmation emails for both the bidder and the admin recipient using the existing SMTP/env pattern, triggered only after a bid is successfully accepted and logged without affecting bid success if mail fails. Why: live auction bidding needed operational notifications, but the bid API had to stay resilient and return success even when email transport is unavailable. Next: if these emails prove useful, add a small winner/finalization email pass later rather than expanding this helper into a broader auction notification system now.
- 2026-03-26: Added a short anti-sniping note to the live PDP auction panel in EN/KA, explaining that a valid bid in the final 10 minutes extends the auction by 10 minutes. Why: the extension rule already existed server-side, but bidders needed to see it clearly in the same panel where they read the countdown and place bids. Next: if the auction UI grows further, keep these operational rules grouped in the same understated metadata area instead of scattering them across the PDP.
- 2026-03-26: Added lightweight PDP auction polling through a small read-only `/api/auction/status` route so the auction block refreshes current bid, next minimum bid, end time, and status every 15 seconds without full realtime infrastructure. Why: the auction countdown and bid form were already in place, but the page still went stale if another bidder changed the auction state from a different browser. Next: if live bidding volume grows, replace polling with a narrower realtime channel only for the active auction event.
- 2026-03-26: Added a live client-side countdown to the PDP auction panel, driven from the current auction end time already loaded into the page, and switch the bid form into an ended display once the timer reaches zero. Why: the auction PDP already showed the bid values and end timestamp, but bidders still needed a clear live time-left signal and the form should stop appearing usable after the countdown expires even before a full page refresh. Next: once the winner/finalization step exists, align the post-countdown UI with the final backend auction state instead of only the client timer.
- 2026-03-26: Added a follow-up migration to fix the auction bid RPC by renaming the PL/pgSQL `current_time` variable to `current_ts`, avoiding a collision with PostgreSQL’s built-in `current_time`. Why: bid attempts were hitting a masked `temporary_error` because the function was comparing a `time with time zone` builtin against the auction `timestamptz` end time. Next: apply this migration, retry the bid, and then confirm any remaining failures are real business-rule responses like `email_mismatch` or `bid_too_low`.
- 2026-03-26: Added inline admin order status updates on `/admin/orders` with a protected server route, strict server-side status validation, and lightweight success/error flash feedback via query params. Why: the first admin orders page was useful for reading, but the next minimal workflow step is changing an order’s state without leaving the list view or introducing a full audit/history system yet. Next: if this works well in practice, the next admin improvement should be per-order detail drill-down or a tiny timestamped status-change note.
- 2026-03-26: Added the first protected `/admin/orders` page with server-rendered order reads, status/order-code/email filters, and simple pagination, plus a dashboard link into it. Why: the admin session is in place now, so the next useful MVP is a functional read-only orders screen without editing or client-side data fetching. Next: if this proves stable, the next admin step can be order detail drill-down or safe status updates.
- 2026-03-26: Added a minimal password-protected admin session with `/admin` login, signed httpOnly cookie auth, proxy protection for `/admin/*`, and a placeholder `/admin/dashboard` page plus logout route. Why: the admin area needs a safe server-side gate now without introducing a full account system or leaking credentials into the client bundle. Next: once the first real admin page is ready, place it under `/admin/*` and rely on the same cookie gate instead of building a separate auth layer.
- 2026-03-26: Hid the normal PDP price block and add-to-basket purchase section for auction items while leaving the auction panel itself in place. Why: auction products should not simultaneously present the standard retail purchase flow and the restricted bidding flow. Next: keep winner/payment handling separate so the auction path stays distinct from the normal painting checkout behavior.
- 2026-03-26: Updated the PDP auction loader so the initial bid display and default bid input reflect the real highest existing bid when one exists, not just the auction starting bid, and clear stale success feedback when the user edits the form again. Why: the first bid UI pass correctly updated after submissions, but a fresh page load still understated the current auction state and could leave an old success message hanging after edits. Next: verify the lamb-easter PDP against a seeded bid to confirm server-rendered current bid, next minimum bid, and success-message clearing all behave correctly.
- 2026-03-26: Added the first PDP auction bid form for live auctions only, with email, order-code, and bid-amount fields posting to `/api/auction/bid`, plus localized success/error handling and live updates for current bid, end time, and minimum next valid bid after each response. Why: the auction backend now exists, so the Lamb Easter painting needed a minimal buyer-only bidding UI without introducing accounts or redesigning the PDP. Next: apply the SQL bid-function migration live, then smoke-test real bid placement and decide whether the next pass should add bid-history visibility or winner-state handling.
- 2026-03-26: Added the first server-side auction bid path with a dedicated `place_auction_bid` SQL function plus `/api/auction/bid`, validating paid-order eligibility (`order_code` + `email`), enforcing minimum increments, rejecting ended/non-live auctions, and extending `ends_at` by 10 minutes for valid late bids. Why: the auction MVP needed a real server-enforced bidding flow before any frontend bid form can be safely exposed, and Supabase JS alone was not transaction-safe enough for concurrent bids. Next: apply the new SQL function migration, then wire the PDP bid form to this route and surface the returned minimum-next-bid / extended end time states.
- 2026-03-26: Surfaced auction state on catalogue cards by loading open `auction_events` into the catalogue product list, showing an auction badge plus current-bid labeling on the relevant card, and moving `painting-lamb-easter` to the second position in the paintings rail. Why: the catalogue needs to visibly reflect that the Lamb Easter painting is being auctioned before bidding is implemented, and its placement should stay prominent among paintings. Next: once bidding exists, swap the temporary starting-bid display for the real highest/current bid from accepted auction bids.
- 2026-03-26: Added read-only PDP auction visibility for products with an open `auction_events` row by fetching the event in `getProductBySlug()` and rendering a small auction block with status, starting bid, minimum increment, end time, and the verified-paid-order eligibility note. Why: `painting-lamb-easter` now needs to visibly read as an auction item to everyone on the site before bid submission is implemented. Next: build the server-side eligibility check and bid-placement API, then replace the read-only note with the restricted buyer bidding flow.
- 2026-03-26: Added a guarded seed migration for the first `painting-lamb-easter` auction event that targets the live product slug but refuses to insert until the real schedule and bid values are filled in. Why: the first auction row should exist in migrations, but the business values are not final enough to hardcode silently or seed with fake defaults. Next: fill the config block with the real `starts_at`, `ends_at`, `starting_bid`, `minimum_increment`, and `winner_payment_deadline_hours`, then apply the migration to create the draft auction row.
- 2026-03-26: Added the first auction MVP schema migration with `auction_events` and `auction_bids`, keeping auction sale-mode separate from `products` and anchoring bidder eligibility to an existing paid order instead of inventing accounts. Why: the upcoming private buyers-only painting auction needs a minimal, safe DB foundation for scheduling events and recording accepted bids before any API or PDP work begins. Next: seed the single lamb-easter auction event, then build the server-side eligibility check and transactional bid-placement path on top of these tables.
- 2026-03-26: Added `'painting'` back to the reconstructed `public.product_type` enum in the core types migration so the repo schema contract matches the existing app logic and live painting product behavior. Why: painting-specific PDP, checkout, cart, and reservation flows already use `product_type = 'painting'` as the source of truth, and the reconstructed migration had drifted out of sync before auction work. Next: keep upcoming auction implementation on top of this normalized painting type instead of introducing any parallel sale-mode workaround.
- 2026-03-26: Added a DB constraint migration for `product_themes` that enforces at most one `is_primary = true` row per product via a partial unique index on `product_id`. Why: the shared theme system already has a primary-theme concept in code and data, so the database should prevent accidental multiple-primary states instead of relying on application discipline alone. Next: apply the migration and run a quick check that no product can insert a second primary theme row.
- 2026-03-26: Rebuilt the homepage hero bottom overlay as one wrapping row with three parts: text, spacer, and CTA buttons, instead of two independently positioned blocks. Why: the previous split absolute layout let the buttons and hero body compete for the same space, while the unified flex row allows the body text to wrap naturally as the CTA area tightens. Next: review the hero at a few desktop widths and on mobile to confirm the body now reflows cleanly around the button stack instead of feeling overlaid.
- 2026-03-26: Changed the homepage hero body from forced manual line breaks to one shared naturally wrapping i18n sentence in EN/KA, so it can stay on one line when space allows and break into additional lines only as the layout tightens. Why: the hero copy should respond to available space instead of being locked to different fixed line counts across breakpoints. Next: review the hero on narrow mobile and desktop widths to confirm the body wraps cleanly without fighting the CTA stack.
- 2026-03-26: Unified the homepage hero body copy across mobile and desktop by removing the desktop hardcoded bypass and making both breakpoints render the same `home.hero.body` i18n value, updated to the final miniatures wording in EN/KA. Why: the hero copy had drifted across breakpoints, so the homepage needed one real source of truth instead of separate mobile and desktop strings. Next: check the homepage in both locales on mobile and desktop to confirm the same wording now appears across breakpoints with the existing layout intact.
- 2026-03-26: Added a follow-up migration to normalize all remaining English couple-motif labels from `Kajari Couple` to `Qajari Couple` across both product titles and collection translations, without changing slugs or Georgian labels. Why: localhost was still surfacing older live English rows with `K`, so the canonical Qajari naming needed to be enforced in every DB-backed user-facing path. Next: apply the migration and verify both couple product titles and any collection-derived labels now read `Qajari Couple`.
- 2026-03-26: Added a follow-up migration to normalize the Georgian shared-theme label for `good_tree_bad_tree` to `ხე კეთილი, ხე ბოროტი` without touching the already-correct composition title wording. Why: the live theme label and future seeded DBs should use the same canonical Georgian naming everywhere user-facing. Next: apply the migration and verify the family-garden PDP shows the updated secondary theme name once theme labels are surfaced.
- 2026-03-26: Switched PDP shared-theme reads from the admin Supabase client to the public read client while keeping the same `product_themes -> catalogue_themes -> catalogue_theme_translations` query and graceful empty fallback. Why: normal product pages should not require service-role environment credentials just to display public theme context. Next: verify one PDP with theme content and one without to confirm the section still renders when available and silently disappears when theme reads fail.
- 2026-03-26: Stacked the homepage hero CTAs vertically on mobile and flipped their mobile-only order so `Catalogue` sits above `Paintings`, while desktop keeps the original horizontal order. Why: the two hero buttons were crowding the hero copy on small screens, and the catalogue link needed a cleaner mobile placement without changing the desktop hierarchy. Next: check the homepage on a narrow device to confirm the stacked buttons no longer cover the hero text and still feel balanced over the image.
- 2026-03-26: Refined the homepage hero and PDP theme-text polish by updating the hero copy/button wording, adding a clearer secondary `Catalogue` CTA treatment, swapping pillow/case order on the homepage category rail, and making the PDP theme text collapse into a single expandable block with mobile/desktop line clamps and chevron affordances. Why: the homepage hero wording and CTA hierarchy needed to read more clearly, while the new shared theme text on PDPs needed a quieter, more compact reading experience. Next: review the homepage hero and one long-theme PDP on mobile and desktop to confirm the CTA balance and expand/collapse behavior both feel natural.
- 2026-03-26: Added a secondary `Catalogue` hero CTA beside the existing paintings/originals button on the homepage image, using a lighter transparent treatment so it stays visually subordinate. Why: the hero now offers a direct path into the full catalogue without overpowering the primary works CTA. Next: check the hero on mobile and desktop to confirm the two-button row still feels balanced over the image.
- 2026-03-26: Moved shared theme text into the PDP buy panel under the price, extended the theme block to support `symbolism_text` and `story_text`, hid `short_description` whenever symbolism copy exists, and added a migration to rename `table-runner-large-family-garden` in KA/EN to its fuller composition title. Why: the new shared theme content should read as quiet purchase context in the right panel, while the family-garden runner title now needs to reflect its actual composition more precisely. Next: once final theme copy is populated, review a few PDPs with primary/secondary themes to confirm the right-panel reading order still feels balanced.
- 2026-03-26: Moved the PDP illustration note into the gallery’s thumbnail strip block and reduced its font size so the disclaimer sits closer to the image context instead of occupying its own separate card below the gallery. Why: the note should feel like supporting media context, not a standalone content panel. Next: check a few phone case, bag, and runner PDPs on mobile to confirm the thumbnail rail and note still breathe well together at narrow widths.
- 2026-03-26: Extended the PDP product loader to read shared theme records from `product_themes` and `catalogue_theme_translations`, then rendered a quiet theme note section under the existing description area showing each linked theme name plus its short description. Why: product detail pages now need to surface the new shared Artiani theme context without replacing or duplicating the existing per-product description field. Next: once the shared theme copy is finalized, decide whether to expose `story_text` and `symbolism_text` as separate PDP sections or keep them behind a later design pass.
- 2026-03-26: Added the shared Artiani theme migration batch covering the `bag-kajari` title fix, new `catalogue_themes` schema, canonical theme seed rows, and initial `product_themes` links for the finalized subject mapping. Why: shared theme/story content now needs a normalized database foundation before the frontend can read reusable theme data across multiple products. Next: apply the migrations live, run the verification queries, and then wire the PDP to read shared theme content alongside the existing product-specific descriptions.
- 2026-03-26: Added the Aversi article card for `ლევან მარგიანი რწმენა და შთაგონება - იგავი ხე კეთილისა და ხე ბოროტის შესახებ` to the artist media dataset and recorded it in a migration. Why: that press/article link should appear in the homepage and biography media rail alongside the other Levan Margiani coverage. Next: if you want richer article cards later, upload dedicated thumbnails into the media bucket so external article entries do not rely on the fallback visual treatment.
- 2026-03-26: Forced PDP navigation to reset the window to the absolute top whenever the product slug changes. Why: opening a product from the catalogue was landing slightly scrolled down instead of starting at the top of the detail page. Next: if any other route transitions show similar behavior, consider centralizing scroll-reset rules rather than patching per page.
- 2026-03-26: Made the catalogue rail arrow buttons visible on mobile instead of hiding them below the `sm` breakpoint. Why: the rail controls were working on tablet/desktop but were explicitly suppressed on phones, so mobile users had no visible arrow affordance. Next: if mobile browsing still feels cramped, consider slightly reducing card width or arrow offset rather than removing the controls again.
- 2026-03-26: Restored the rectangular tablecloth print-area note for the `140` widths even on white variants, while still keeping the existing hidden cases for the smaller white rectangular/round combinations. Why: the larger rectangular cloths still need their reduced print-area explanation regardless of background color. Next: if print-area disclosure rules keep getting more product-specific, move these size exceptions into shared print-area metadata instead of growing the PDP condition inline.
- 2026-03-26: Added a small info button on the first PDP image that opens the same product note already shown beneath the gallery, while preserving the existing product-specific note variants for bags, phone cases, and other items. Why: customers can now access the image disclaimer directly from the gallery without duplicating or hardcoding note text per product family. Next: if the new info affordance feels useful in testing, consider whether the under-image note can eventually be visually softened since the same copy is now accessible from the image itself too.
- 2026-03-26: Corrected the Georgian homepage delivery trust-bar copy so both Tbilisi and region timelines explicitly use `სამუშაო დღეში`. Why: every delivery-time mention should use the same phrasing, and the homepage region text had an abbreviated `5–6-ში` that was inconsistent with the rest of the site. Next: keep future delivery-copy edits aligned across the homepage, checkout, and delivery page so timing language does not drift between surfaces.
- 2026-03-26: Changed the bag PDP care note to show only `30°C washable` while leaving the broader `30–40°C` note in place for the other non-painting products. Why: bags need a stricter washing instruction without changing the care copy used elsewhere in the catalogue. Next: if more product families get custom care rules later, keep them behind shared PDP note variants rather than hardcoding category text in the component.
- 2026-03-26: Restored the homepage category-card image mappings for phone cases and bags to `.jpeg` after reuploading those assets in storage. Why: the category card paths should stay aligned with the canonical `category-cases-card.jpeg` and `category-bags-card.jpeg` filenames used by the homepage carousel. Next: keep future category-card uploads on the same `.jpeg` filenames so the shared mapping does not drift again.
- 2026-03-26: Moved the homepage social-follow block to sit directly beneath the About Artiani button in the intro section. Why: the CTA should come first, with the Facebook and Instagram links grouped immediately after it instead of interrupting the reading flow above the button. Next: review the spacing on mobile to make sure the button and follow links still feel balanced within the section.
- 2026-03-26: Reordered the homepage category cards to follow the requested sequence of tablecloths, phone cases, pillows, and scarves before any remaining categories. Why: the homepage category carousel should reflect the manually chosen merchandising order instead of the fallback backend category sort. Next: after the new case and bag card assets are uploaded, verify the homepage still reads well if bags or runners appear later in the rail after the prioritized categories.
- 2026-03-26: Added homepage category-card image mappings for the new `phone_case` and `bag` categories, pointing them to `category-cases-card.jpg` and `category-bags-card.jpg` in product storage. Why: the homepage/category carousel needs stable shared image paths for these new categories instead of falling back to product imagery. Next: once the new assets are uploaded, verify the homepage card order and crop feel right against the existing tablecloth, pillow, and scarf category cards.
- 2026-03-26: Tightened the catalogue rail controls so arrows appear only when a row truly overflows, hide automatically at the start/end positions, and use a softer beige edge fade instead of the harsher white overlay. Why: the first control pass improved navigation, but it still showed unnecessary arrows on short rows and the white fade looked too artificial against the catalogue surface. Next: if needed later, add disabled-state motion or progress dots only after confirming the current scroll-state logic feels clear enough on touch devices too.
- 2026-03-26: Refined the new catalogue rails with edge fades, native left/right arrow controls, and wider card-peek sizing so more of the next product stays visible in each row. Why: the first horizontal-rail pass worked, but the next-card cue was still too subtle and the catalogue needed clearer affordances without introducing a heavy JS carousel. Next: if browsing still needs stronger momentum cues later, tune the scroll distance or add small progress indicators before considering any more complex carousel behavior.
- 2026-03-26: Replaced the catalogue page’s per-category multi-row product grids with single-line horizontal scroll rails sized to keep a partial next card visible at each breakpoint. Why: the catalogue should browse as continuous horizontal rows rather than wrapping into stacked grids, while still signaling that more items are available off-screen. Next: if users need stronger navigation cues later, add lightweight arrow affordances or edge fades without replacing the native scroll behavior.
- 2026-03-26: Added a repo-backed phone-case model dataset with fallback fetching, wired a required phone-model selector into phone-case PDPs, carried the selected model through cart/checkout/order pricing/email details, and disabled phone-case quick-add from catalogue cards so the required selector cannot be bypassed. Why: phone cases now need an explicit device-model choice stored as part of the actual order configuration rather than an implied or missing option. Next: once the new Supabase migrations are applied live, remove the fallback-list warning path only after confirming the table contents and PDP selector stay in sync with the approved model set.
- 2026-03-26: Changed PDP recommendations to pull from the full active catalogue for every product type instead of preferring same-category items first. Why: bags, phone cases, and every other product detail page should be able to surface any random catalogue item rather than staying inside their own product family. Next: if recommendation quality needs tuning later, add explicit cross-category weighting or exclusions rather than restoring the old category-only path.
- 2026-03-26: Extended the shortened PDP illustration note to bags as well, so both bags and phone cases omit the final “see the detail photos” sentence while other non-painting products keep the full note. Why: bags need the same shorter disclaimer treatment as phone cases without carrying a phone-case-specific dictionary key. Next: if more categories need custom PDP disclaimers, keep them grouped by shared copy variants instead of creating one key per product type.
- 2026-03-26: Set all phone case variant prices to `40 ₾` and added a matching Supabase migration so repo state stays aligned with the live catalogue. Why: phone cases were still priced at `35 ₾` in the product variants backing both catalogue cards and PDP pricing. Next: if phone cases later need model-specific pricing, move from one shared product-type price to explicit per-variant pricing rules instead of another bulk update.
- 2026-03-26: Removed the last-sentence “see the detail photos” copy from the shared PDP illustration note for phone cases only, while leaving the fuller note in place for other non-painting products. Why: phone cases should keep the standard illustrative-product disclaimer without the extra direction to the last image. Next: if more product families need tailored PDP disclaimers later, keep them dictionary-driven by product type instead of forking the whole note block markup.
- 2026-03-26: Hid the washable-care note on phone case product detail pages while keeping it visible for other non-painting products. Why: the shared care copy was being inherited by phone cases even though it does not apply to that product type. Next: if more product-type-specific care guidance is needed later, split the single shared note into per-type detail copy instead of adding more exceptions in the panel.
- 2026-03-26: Split phone cases out of the fallback `other` grouping by mapping `phone_case` to its own catalogue category label and matched their catalogue cards to the bag-style white contained image framing. Why: the new phone case products should not read as miscellaneous items, and their upright artwork needed more breathing room than the default cover crop gave them. Next: when the model selector is added later, keep it within this dedicated phone-case group instead of reusing the generic other-items flow.
- 2026-03-26: Polished the new bag-product experience by giving bag catalogue cards a cleaner white object-contain frame, moving the PDP color swatches under the image instead of overlaying them, and fixing catalogue default-variant selection so live `is_default` bag colors actually drive the card preview image. Why: the bag import exposed two presentation issues at once: the cards looked cramped with cover-cropped images, and the catalogue was ignoring the configured default bag color and always falling back to the first variant. Next: after adding more bag families, do a quick visual pass to confirm the rotated default colors still give a balanced catalogue row and that the under-image swatch block feels stable on both desktop and mobile.
- 2026-03-25: Tweaked the mobile painting zoom toggle so turning zoom on immediately shows the lens centered over the image instead of waiting for the first touch move. Why: the toggle felt incomplete when users enabled zoom but saw no immediate visual response. Next: if the default centered position feels off on some paintings, adjust the initial lens anchor rather than reworking the toggle flow.
- 2026-03-25: Added an explicit mobile painting zoom toggle with EN/KA labels so touch users can switch the lens on only when needed, keeping normal swipe browsing intact by default and avoiding accidental zoom activation. Why: always-on touch magnifier behavior was too intrusive for the painting gallery, while the zoom interaction itself is still useful on mobile. Next: if mobile testing shows the toggle is visually too prominent, reduce its weight rather than removing the opt-in zoom control.
- 2026-03-25: Refined the last hero and painting-zoom details by locking the Georgian mobile hero subhead into three explicit lines, forcing the Georgian desktop subhead into a cleaner two-line break, and offsetting the touch magnifier upward so the user’s finger does not cover the lens. Why: the remaining issues were visual polish problems caused by text reflow after load and by the touch lens sitting directly under the finger. Next: only revisit the hero copy measure or touch-lens offset if real device testing still shows awkward overlap on specific screen sizes.
- 2026-03-25: Extended the painting PDP magnifier to touch devices with a press-and-drag lens interaction, and hid the shared description panel for paintings so only their structured size/material facts remain visible. Why: painting detail pages needed a usable close-look interaction on mobile too, while the old copied description block had become redundant and misleading next to the newer painting metadata. Next: if the touch magnifier feels too sensitive in real use, tune the long-press delay and lens size instead of changing the gallery layout.
- 2026-03-25: Added a painting-only desktop hover magnifier to the PDP gallery, enlarged the magnifier lens for closer inspection, and made small homepage hero mobile tweaks so the Georgian subhead breaks more cleanly while the single Paintings CTA sits slightly higher from the frame. Why: paintings needed a closer-look interaction without bringing back fullscreen behavior, and the hero still needed minor mobile polish after the latest art-direction changes. Next: verify the magnifier on a few painting PDPs in a real browser and only tune lens size/zoom further if it feels either too jumpy or too weak in use.
- 2026-03-25: Removed fullscreen opening from the shared PDP gallery, hid the bottom-left swatch picker for paintings, and updated the live painting set so `painting-family` was removed and replaced by a new `painting-lamb-easter` product while painting cards now prefer `-front-thumbnail` images. Why: the painting PDP needed a quieter, non-lightbox image experience and the live painting catalogue needed to match the newly uploaded media set. Next: if more one-off painting variants are added later, follow the same product/image pattern and keep thumbnail naming consistent with the `-front-thumbnail` convention.
- 2026-03-25: Swapped the live painting products `painting-good-shepherd` and `painting-lamb` to the newly uploaded `front`/detail storage assets, deleted their replaced old PNG files from storage, and taught catalogue painting cards to prefer the new `-front-thumbnail` images. Why: the painting catalogue and PDPs needed to move to the new image set without touching unrelated product media or the hero assets. Next: if `painting-family` also gets a refreshed image set later, follow the same row/storage pattern instead of overloading the current one-off old file.
- 2026-03-25: Removed the positive `Available` badge from paintings so only sold/out-of-stock paintings carry a status badge in catalogue cards and on the painting detail gallery. Why: the always-on green badge added unnecessary noise to one-of-a-kind works, while the sold state still needs explicit visibility. Next: keep the painting status treatment quiet unless real stock states beyond sold become operationally necessary.
- 2026-03-25: Replaced the checkout page’s native browser confirm with a small in-page delivery-details confirmation modal that shows the current name, phone, email, and address before the order request is sent. Why: the Chrome default confirm looked out of place against the site and felt too abrupt for checkout. Next: if needed later, add focus-trapping and Escape-key close behavior without changing the current order flow.
- 2026-03-25: Added a final client-side checkout confirmation prompt for name/phone/email/address before submitting the order, switched the Georgian homepage hero title to the English `Art you can live with.`, forced the English hero subhead to break after `collectible`, restored cleaner `Available` wording, and let the desktop hero title stay on one line. Why: the remaining friction was mostly small trust and polish issues rather than business logic gaps. Next: watch the live checkout flow once orders start coming in and only replace the native confirmation dialog if it proves too abrupt in real usage.
- 2026-03-25: Finalized a small Georgian checkout-copy cleanup by removing the extra checkout intro line, reordering the payment/delivery note ahead of the painting-specific hold note, tightening `ჯავშნდება` to `იჯავშნება`, and ignoring local `supabase/.temp/` CLI noise in `.gitignore`. Why: the checkout copy needed to read in a calmer order and the repo still had noisy untracked Supabase temp state. Next: leave the checkout layout as-is unless a real usability issue shows up in live orders.
- 2026-03-25: Polished a small set of visible EN/KA storefront strings across the homepage hero, trust bar, catalogue intro/empty states, delivery and returns guidance, and painting availability microcopy to make the tone calmer, clearer, and more premium without changing layout or business logic. Why: the site already had the right structure, but a few lines still felt generic, heavy, or awkward for an artist-led premium shop. Next: leave the rest of the longer editorial copy alone for now and revisit only after more real customer feedback shows where wording still causes hesitation.
- 2026-03-25: Added a minimal client-side analytics abstraction and wired it into product views, successful add-to-cart actions, checkout entry/confirmation steps, and successful order creation without sending customer PII. Why: launch needs a small, consistent event layer for core commerce behavior before any heavier analytics integration. Next: connect this abstraction to the chosen analytics backend and only add `order_paid_confirmed` once there is a real in-app payment-confirmation moment to emit it safely.
- 2026-03-25: Removed lingering `Small` / `Large` runner prefixes from shared runner display paths and product-page related-card labels, and tightened the Georgian returns eyebrow wording to `მომხმარებელზე ზრუნვა`. Why: runner cards were still exposing internal size-style labels in user-facing titles/overlines on suggested products, and the returns page eyebrow needed the approved Georgian phrasing. Next: if any runner naming still looks off in live browsing, inspect the underlying subtype taxonomy separately rather than reintroducing display-time prefixes.
- 2026-03-25: Added product-page JSON-LD for a single `Product` plus `BreadcrumbList`, using the existing localized product route data and real stock-state-derived availability instead of the earlier broader variant-group schema. Why: launch needed cleaner, more targeted product SEO without a full metadata overhaul. Next: in the later SEO pass, consider enriching product schema with more precise per-variant semantics or shipping/return policy data only if the business rules are finalized.
- 2026-03-25: Added basic App Router SEO infrastructure with a localized sitemap and robots definition driven by `PUBLIC_BASE_URL`, including public home/catalogue/product/about/delivery/returns pages while keeping utility surfaces like cart, checkout, track, and API routes out of indexing. Why: launch needed a clean, explicit crawl surface without a broader SEO rewrite. Next: in the later SEO pass, review canonical metadata breadth, last-modified signals, and whether any collection/filter URLs deserve inclusion.
- 2026-03-25: Simplified the footer contact presentation again by moving the email address directly under the Help and support copy with a small Contact label, instead of keeping it in a separate footer block or under quick links. Why: the previous footer contact treatment still felt visually heavy and disconnected from the support copy. Next: do one more visual pass on mobile and desktop to confirm the footer now feels balanced without losing clarity.
- 2026-03-25: Added a dedicated Returns & Exchanges page, reorganized the footer into a calmer trust-and-links structure with email-only contact, removed phone/social links from sitewide help and navigation surfaces, and moved Facebook/Instagram presence to the homepage About Artiani block and the About page only. Why: the site needed a more complete trust layer and clearer support navigation without cluttering the header or making contact areas feel inconsistent. Next: review the soft-launch return/exchange wording manually for business and legal fit before launch.
- 2026-03-25: Simplified two small navigation/help affordances by making the desktop header flag switch language immediately instead of opening a locale menu, and by changing the delivery-page tracking note to refer to the clickable profile page link with slightly stronger link emphasis. Why: with only two active locales, the extra desktop dropdown was unnecessary friction, and the delivery guidance needed to match the current “profile” wording while giving users a direct path to tracking. Next: if more languages ever return, revisit the direct-switch header behavior before reintroducing a desktop locale menu.
- 2026-03-25: Added a compact post-add cart confirmation flow with direct `View basket` and `Checkout` actions by introducing a reusable `CartToast`, wiring it into catalogue-card and PDP add-to-cart interactions, and extending the feedback hook with an explicit close path. Why: the previous add flow only flashed a short “added” state and did not clearly tell users what to do next, especially on mobile and desktop catalogue browsing. Next: if the add flow needs more merchandising later, consider optionally showing item count or subtotal in the toast without turning it into a heavy mini-cart.
- 2026-03-25: Refined the new homepage hero for readability and mobile art-first balance by removing extra overlay lines, tightening the Georgian text breaks, making the first CTA say `ნამუშევრები`, strengthening secondary CTA contrast, moving only two CTAs into the mobile image overlay with the third below, repositioning the mobile title to the top of the hero, and placing the trust cards below the category row with a tighter 2-up mobile / 4-up desktop grid. Why: after the initial homepage reframing, the hero still felt too text-heavy and button-heavy on mobile, and some trust/CTA elements needed clearer hierarchy and contrast. Next: visually review the homepage on a real phone to confirm the hero image now has enough breathing room and the trust-card copy density still feels calm.
- 2026-03-25: Reframed the homepage above the fold to feel more shop-first and premium by replacing the old category-only banner with a stronger art-led hero, direct collection CTAs, and a compact trust bar covering delivery, transfer flow, collectibility, and customer help. Why: the homepage needed to reduce the wall-of-text feeling and make the storefront intent clearer without abandoning the museum-minimal tone. Next: review the new hero on mobile and desktop to confirm the CTA balance and trust-bar density feel right against the existing category carousel.
- 2026-03-25: Added `PUBLIC_BASE_URL` to the GitHub Actions release-gate workflow env so prerendered pages can resolve the public site URL during CI builds. Why: the build was failing in GitHub Actions because `getPublicBaseUrl()` had no non-localhost URL available in CI even though the Supabase envs were already configured. Next: rerun the Release Gate workflow and confirm the build passes with the new secret wiring.
- 2026-03-25: Updated the painting payment/hold copy across the PDP, checkout, delivery-info page, and order tracking to use a 20-minute transfer window and added the instruction to email the transfer receipt to `app.droze@gmail.com` when sending money from another bank. Why: interbank transfers can take longer to appear, so the painting hold messaging needed to reflect the longer safe window and give customers a clear fallback proof path. Next: once the final operational support email is locked, keep this address aligned across checkout, delivery guidance, and any future payment emails.
- 2026-03-25: Replaced the About Artiani EN/KA brand text with the newly approved father-and-son copy and updated the homepage Artiani excerpt to use only the new opening paragraph. Why: the brand story needed the exact latest wording on the About page, while the homepage needed a shorter lead-in from the same text. Next: review the homepage and About page in both languages to confirm the new paragraph rhythm feels right in the current layout.
- 2026-03-25: Added painting sold-state protection end to end by reusing `product_variants.stock_status` for catalogue/PDP status badges, disabled add-to-basket on sold paintings, rejected sold paintings in cart validation and final server repricing, and added a new localized `/{lang}/delivery` page linked from navigation with payment and delivery guidance for products and paintings. Why: one-of-a-kind paintings need a real unavailable state enforced beyond the UI, and launch needed a clear standalone page explaining transfer, delivery, painting reservation timing, and tracking. Next: when a painting is confirmed sold, make sure its variant `stock_status` is updated operationally so the new restriction and badges activate immediately in production.
- 2026-03-24: Verified the live runner defaults are already canvas in Supabase and updated the PDP runner material-option ordering to follow canonical material sort order so canvas renders before velvet in the chooser. Why: the actual default runner variants were already correct in live data, but the info-panel material buttons still inherited variant ordering and could surface velvet first even though canvas is the intended primary material. Next: after the missing runner/scarf image assets are restored, verify a few runner PDPs to confirm they open on canvas by default and list canvas before velvet in every color group.
- 2026-03-24: Fixed the homepage tablecloth and pillow category-card paths to match the actual live storage object extensions (`.jpg` instead of `.png`). Why: runners and scarves were already using valid canonical filenames, but cloth and pillow cards were pointing at non-existent `.png` objects, which left those homepage cards blank even though the images existed in storage. Next: if more category-card assets are re-exported later, keep the storage extension and frontend reference aligned or normalize them in storage to a single format first.
- 2026-03-24: Normalized the couple-motif display naming in live catalogue collection translations so the relevant KA/EN labels now consistently read `ყაჯარები` and `Kajari Couple`, and added a repo migration to keep existing environments aligned. Why: the visible couple motif was already correct at the product-title level, but collection display rows still mixed `წყვილი / Couple` and `Qajarebi`, which could leak inconsistent naming into UI surfaces that use collection labels. Next: if the storefront starts rendering collection names more broadly later, keep those surfaces on the same normalized KA/EN labels and leave the single-figure `kajari` naming unchanged.
- 2026-03-24: Added circular `lilac` to the cloth large-main canonical asset set and frontend coverage so rounded lilac cloth PDPs now use the large main image instead of falling back to the normal main image. Why: that large lilac source asset already existed in storage, so excluding it was an implementation gap rather than an intended fallback case. Next: verify the rounded lilac cloth PDP specifically to confirm the main image now resolves to the canonical large asset while the detail image stays unchanged.
- 2026-03-24: Added a one-off Supabase Storage copy script for canonical cloth large-main and category-card asset names, then simplified the frontend to build cloth large-image paths directly from the real background codes and point category cards at the canonical runner/scarves filenames. Why: storage filenames should match the DB-backed naming so the PDP and category-card image logic can stay direct and predictable without special-case alias translation in the frontend. Next: keep the old storage aliases in place for safety for now, then delete them in a later cleanup pass once the canonical names have baked in production.
- 2026-03-24: Extended the white rectangular tablecloth print-area-note exceptions so the safe-zone card is also hidden for white `140x200` and `140x240` selections, while keeping the existing `110x110` all-color and white `130x130` exceptions. Why: those larger white cloth variants should use the same no-note treatment as the other white rectangular cases. Next: verify white rectangular `130x130`, `140x200`, and `140x240` variants plus a colored large rectangular cloth to confirm the note only appears on the intended combinations.
- 2026-03-24: Added a cloth-only PDP gallery override that swaps just the resolved `main` image to the uploaded `*-large-main.png` asset for eligible large round/rectangular cloth selections, while keeping `110x110`, white `130x130`, detail images, and all non-cloth products unchanged. Why: larger cloth variants need the dedicated large main visual without disturbing the existing gallery/detail-image behavior or any DB-backed image data. Next: manually verify round and rectangular cloth PDPs across `110x110`, white `130x130`, non-white `130x130`, and larger rectangular sizes to confirm the main image switches only in the intended cases.
- 2026-03-24: Adjusted the Georgian homepage Artiani headline to use `მინიატურები` instead of the previous genitive phrasing. Why: the approved brand line needed the display wording corrected in the visible homepage title. Next: verify the Georgian homepage headline reads naturally in the current layout.
- 2026-03-24: Replaced the homepage Artiani excerpt and About page EN/KA brand copy with the final approved miniature-focused text, while keeping the existing CTA, route, and page structure unchanged. Why: the brand story needed the final approved wording without expanding the homepage block or changing the About page layout. Next: review the homepage and About page in EN/KA to confirm the final text rhythm feels right within the existing typography.
- 2026-03-24: Normalized the Georgian scarf terminology across the active frontend copy to `თავსაფარი / თავსაფრები` and verified live Supabase no longer contains deprecated Georgian alternatives in the scarf-related translation tables. Why: the project should use one consistent Georgian product term for this category across UI and data-backed copy. Next: if more scarf-specific Georgian marketing copy is added later, keep it aligned to this same terminology.
- 2026-03-24: Widened the homepage Artiani excerpt title measure so the new brand headline wraps in a cleaner 2-3 line rhythm instead of collapsing into a narrow stacked block. Why: the updated copy is longer than the previous headline and needed a broader text measure to stay editorial and readable. Next: verify the homepage excerpt on mobile and desktop to confirm the title now breaks cleanly without dominating the section height.
- 2026-03-24: Rewrote the homepage Artiani excerpt and the existing About route with new EN/KA brand-led copy, added a CTA from the homepage excerpt to `/{lang}/biography`, and appended the homepage media rail to the About page. Why: the brand story needed to feel more exclusive, collaborative, and emotionally tied to bringing Levan Margiani’s works into everyday life without redesigning the existing pages. Next: review the EN/KA homepage excerpt and About page in browser to confirm the new paragraph rhythm and media block placement feel balanced.
- 2026-03-24: Moved the shared PDP description panel into the left media column for all products, directly under the gallery stack and illustration note instead of rendering it as a full-width block below the page. Why: scarves and other PDPs should keep long-form content aligned with the image column rather than stretching the description across the full layout width. Next: verify a scarf PDP and a non-scarf textile PDP to confirm the description now sits under the media stack and before the related products grid.
- 2026-03-24: Fixed the shared PDP static material row so single-material products now show the label `Material` instead of `Choose material`; single-size sections were already using `Size`. Why: informational rows should not use chooser wording when there is no real choice. Next: verify a scarf PDP and another single-material PDP to confirm the material label no longer says `Choose`.
- 2026-03-24: Adjusted the scarf PDP info panel so the one-sided print note now appears directly after the material value in parentheses instead of as a separate row. Why: that production detail reads more cleanly as part of the material line for scarves. Next: verify a scarf PDP to confirm the material line now reads with the one-sided note inline.
- 2026-03-24: Adjusted shared PDP size/material section labels so single-option sections read as `Size` / `Material` instead of `Choose size` / `Choose material`, while multi-option sections keep the chooser wording. Why: single available options are informational, not real choices. Next: verify a PDP with one material option and another with multiple material options to confirm the label wording switches correctly.
- 2026-03-24: Reordered the PDP buy-panel option sections so material now appears before color/style selection, without changing any option logic or availability. Why: the material choice should come earlier than color selection in the product-detail flow. Next: verify a runner PDP to confirm the info panel now reads Material before Color.
- 2026-03-24: Removed the social/artist links block from the PDP buy/info panel so the product detail sidebar stays focused on product information and purchase actions only. Why: the social links were adding extra noise inside the product details panel. Next: verify a few PDPs to confirm the sidebar spacing still feels balanced without that block.
- 2026-03-24: Tightened the rectangular tablecloth PDP print-area-note exceptions to the exact requested cases: hide the note for `110 × 110` in any color and for white `130 × 130`, while keeping it for other reduced-print rectangular size/color combinations. Why: the broader rectangular white-cloth exception was hiding the note on cases where it should still be shown. Next: verify rectangular `110 × 110`, white `130 × 130`, and at least one colored larger rectangular cloth on the PDP to confirm the note appears only on the intended variants.
- 2026-03-24: Extended the white-cloth PDP print-area-note exception to rectangular tablecloth variants too, so white reduced-print rectangular cloths no longer show the `ბეჭდვის სიგანე: 110 cm` warning card. Why: the reduced-print note should stay focused on colored cloth cases rather than white tablecloth variants. Next: verify one white rectangular cloth, one white round cloth, and one colored large cloth to confirm the note now appears only on the intended variants.
- 2026-03-24: Narrowed the PDP print-area note so round white tablecloth variants no longer show the `110 × 110 cm` reduced-print warning card; the note now stays for variants where the print area is actually reduced and the round cloth is not white-like. Why: the warning card should only appear for the large colored cloth cases that need it, not for white round cloth variants. Next: verify one large colored round cloth and one white round cloth on the PDP to confirm the note only appears on the intended variant.
- 2026-03-24: Extended the same white long-description panel treatment to non-painting PDPs as well, keeping only the placement difference where paintings stay under the gallery and other products keep the block below the two-column layout. Why: the description surface should feel consistent across product details even when its placement differs by product type. Next: verify one painting PDP and one textile PDP to confirm the shared panel styling matches in both placements.
- 2026-03-24: Softened the shared PDP buy-panel section labels across products so option/fact titles now read lighter than their values instead of competing with them. Why: the same quieter label treatment used for painting facts was needed across the rest of the info panel for consistency. Next: verify a textile PDP and a painting PDP to confirm the shared label hierarchy now feels consistent.
- 2026-03-24: Reduced the catalogue grouped category heading scale again so section titles sit even more quietly above the grid. Why: the previous reduction still left the category titles larger than desired for the catalogue rhythm. Next: check the catalogue visually to confirm the smaller headings still separate sections clearly.
- 2026-03-24: Reduced the catalogue grouped category heading scale once more so section titles sit more quietly above the product grids without changing grouping or grid behavior. Why: the catalogue category titles still felt slightly too large relative to the tightened card layout. Next: verify the catalogue on mobile and desktop to confirm the smaller heading size still anchors each section clearly.
- 2026-03-24: Corrected the Georgian singular label for the `works` category to `ნამუშევარი` in the repo fallback and taxonomy seed so PDP/product-type naming stays singular while plural list labels remain `ნამუშევრები`. Why: the singular category value for paintings/works was incorrectly seeded as plural. Next: keep the live Supabase translation row and any future taxonomy seeds aligned on this singular/plural split.
- 2026-03-24: Simplified the PDP detail block to use only the long translated product description field as a dedicated white-surface content panel, removing the extra secondary detail column from that area so material-related copy is not shown there. Why: the detail area should read as a clean long-description block sourced from the product tables rather than a mixed information section. Next: verify a few PDPs to confirm the new white description panel reads well for both paintings and textiles.
- 2026-03-24: Switched the PDP info-panel subtitle under the product title to prefer singular `catalogue.types.*` labels instead of category naming, while leaving cart/order-facing product type labels on the existing product-label path. Why: the detail-page subtitle should not show plural category wording. Next: verify a few PDPs like paintings, runners, pillows, and tablecloths to confirm the subtitle now reads as a singular product type.
- 2026-03-24: Softened the painting PDP fact labels for Size and Material so the field titles read lighter and quieter than the actual values in the buy panel. Why: the painting fact headings were visually competing too much with the values. Next: verify a painting PDP in the browser to confirm the label/value contrast now feels clearer.
- 2026-03-24: Made painting cart behavior one-off only and moved painting PDP details under the gallery column: paintings now store `productType` in cart state, ignore repeat adds/incrementing beyond 1, hide basket quantity controls, and render the description/care block directly beneath the image column instead of below the right info panel. Why: paintings should behave like singular works, not quantity-based goods, and their supporting text belongs under the artwork rather than under the buy panel. Next: verify a painting PDP plus cart flow to confirm repeated add-to-cart clicks do not increase quantity and the description block now sits under the gallery only for paintings.
- 2026-03-24: Hid the PDP illustration-note block for painting products only, while leaving the note in place for textile PDPs. Why: the illustration disclaimer should not appear on painting product pages. Next: verify one painting PDP and one textile PDP to confirm the note now appears only on textiles.
- 2026-03-24: Added a painting-only PDP branch so painting products no longer render selectable color/material controls in the buy panel and instead show static localized Size and Material facts from the active variant, with the material line sourced only from canonical `materialInfo.name`. Why: paintings should behave like fixed works rather than configurable textiles, and the buy panel needed to stop showing option UI or legacy free-text material labels for them. Next: verify a real painting PDP in KA/EN/RU to confirm the static facts render correctly while textile PDP selectors remain unchanged.
- 2026-03-24: Reduced the catalogue section heading scale again so grouped category titles read a bit smaller across mobile, tablet, and desktop without changing layout, grouping, or product-card behavior. Why: the category titles on the catalogue page were still visually larger than needed relative to the tightened grid. Next: verify one desktop and one mobile catalogue view to confirm the smaller headings still anchor each section clearly.
- 2026-03-24: Switched listing-card secondary text from product description to `product_translations.subtitle`, rendering it as a subtle single-line subtitle beneath the product title on the shared catalogue product card only. Why: listing cards should show the short translated subtitle field rather than longer descriptive copy, while keeping card heights stable and the diff minimal. Next: verify a few subtitle/no-subtitle products in both Georgian and English to confirm the one-line clamp stays balanced across the catalogue grid.
- 2026-03-24: Reduced the catalogue product card scale slightly by shortening the image block and trimming title, description, and price padding/type sizes, without changing the catalogue grid, product logic, or card actions. Why: the previous pass still left the cards feeling a bit larger than needed inside the tightened editorial catalogue layout. Next: verify a few 4-column desktop sections and 2-column mobile sections to ensure the smaller card scale still keeps descriptions readable.
- 2026-03-24: Re-tuned the catalogue page only with a slightly wider `~1440px` content container, tighter chip and section rhythm, smaller editorial headings, a firmer 2/3/4-column grid with 16px/24px gaps, and denser 16px card content spacing. Why: the catalogue still felt a bit airy on desktop and the section headings/cards carried more visual weight than the requested premium editorial balance. Next: review one dense desktop category and one mobile category section to confirm the new heading scale and tighter card copy still feel comfortable in both Georgian and English.
- 2026-03-24: Nudged catalogue cards slightly smaller after the layout pass by using a square image crop and trimming title/description/price spacing and type scale, while keeping the 2/3/4-column catalogue grid and existing card actions unchanged. Why: the widened catalogue layout looked better, but the cards still wanted a touch more restraint to feel balanced across desktop and mobile. Next: check a few dense catalogue sections on large desktop to confirm the new card scale still feels premium without making descriptions too terse.
- 2026-03-24: Added product descriptions to catalogue cards as a muted two-line excerpt beneath each title while keeping the earlier tightened grid and card rhythm intact. Why: the catalogue cards needed a little more editorial context without changing product logic, grouping, or making the grid feel loose again. Next: review a few long-description products on mobile to confirm the two-line clamp stays balanced across languages.
- 2026-03-24: Tightened the catalogue page layout only by widening the centred content container, reducing section and heading spacing, and refining catalogue card rhythm so grouped product sections use desktop width better while staying clean on mobile. Why: the catalogue felt too left-heavy and vertically loose, with oversized section headings and cards that read less premium than the rest of the site. Next: review the live catalogue on a few desktop and phone widths to decide whether the widened container should stop here or gain a slightly larger 4-column breakpoint later.
- 2026-03-24: Updated the `table_runner` category card image URL to use `category-runner-card.png` from Supabase storage. Why: the homepage/catalogue category card needed to point at the requested replacement asset without changing category structure or UI behavior. Next: verify the updated image renders correctly on the localized home and catalogue category surfaces in the running app.
- 2026-03-21: Added `plural_name` to category translations and wired catalogue/home list surfaces to prefer plural category labels while keeping singular category names for PDP and other singular contexts. Why: category-list UI needed data-driven plural labels without changing the underlying taxonomy model or singular product-detail behavior. Next: after the DB migration is applied, verify the seeded plural labels render correctly in catalogue filters, catalogue headings, and homepage category labels for all locales.
- 2026-03-21: Narrowed SEO material handling so structured data now prefers the canonical translated variant material name from `materialInfo`, then falls back to legacy `variant.material`, and no longer uses `product.materialDescription` as the primary material label. Why: product-level material description is descriptive copy, not a reliable canonical material identifier. Next: once canonical material coverage is complete, remove remaining legacy material fallbacks from non-PDP/SEO surfaces in a separate pass.
- 2026-03-21: Added a repo-backed shared material system for product variants by introducing `catalogue_materials`, `catalogue_material_translations`, and `product_variants.material_id`, seeding the approved canonical materials with KA/EN/RU names, and wiring catalogue/PDP models so product-detail material text prefers the canonical translated material name while still falling back to legacy `material`. Why: materials needed the same canonical DB treatment as backgrounds, but product detail still had to behave correctly for variants that only carry old text or no material at all. Next: once real variants are populated with `material_id`, remove the remaining legacy material-text fallback paths from the PDP and later from other variant-dependent helpers.
- 2026-03-21: Added a repo-backed shared background system for product variants by introducing `catalogue_backgrounds` plus `product_variants.background_id` migrations, seeding the approved canonical backgrounds, and wiring catalogue/PDP models so product-detail swatches prefer DB-backed color/image backgrounds while still falling back to legacy `background_name`. Why: background swatches were still derived from a hardcoded frontend map, but the product background set now needs a canonical reusable DB source without breaking current variants that still only have `background_name`. Next: once real variants are populated with `background_id`, remove the remaining hardcoded fallback matching from the swatch path and, later, shrink other `background_name`-only code paths.
- 2026-03-21: Centralized the taxonomy display/fallback helpers by moving shared product label building, legacy category/subtype fallback resolution, and category grouping into `catalogueModels`, then switched catalogue/home/PDP call sites to consume those shared helpers without changing behavior. Why: the taxonomy migration had left the same category-grouping and display-label logic repeated across `catalogueQueries`, `CatalogueGrid`, `HomePageView`, `ProductCard`, and `ProductDetailView`, which made future cleanup harder while the repo still needs legacy `product_type` compatibility. Next: once real products are repopulated under the new taxonomy, remove the remaining `product_type`-only compatibility branches in print-area, SEO, email, and order lookup.
- 2026-03-21: Removed dead merged-category remnants after the taxonomy switch by deleting the unused `isWhiteLikeColor` print-area helper and pruning unreferenced catalogue i18n keys for the old `cloths/runners/pillows/scarves` filter/common labels plus the unused `catalogue.shapes.*` strings. Why: those merged-category and shape keys were no longer referenced anywhere after the catalogue moved to taxonomy-backed categories, so keeping them would only preserve stale IA in the repo. Next: in the next cleanup pass, centralize the remaining taxonomy label/fallback logic and then trim legacy `product_type`-only helpers once real products are repopulated under the new model.
- 2026-03-21: Migrated the app’s catalogue/category view model onto the new taxonomy path by loading category and collection tables in `catalogueQueries`, exposing `category`, `subtype`, and `collection` data on `CatalogueProduct`, switching catalogue filters and homepage category grouping to real category rows, and updating PDP labels/print-area logic/SEO to prefer taxonomy fields while keeping `product_type` as a fallback for legacy or pre-migration database states. Why: the approved catalogue model needed to become the app’s source of truth before any future UI redesign, but the current live DB may still lag the repo migrations, so the code had to remain compatible with missing taxonomy columns and tables. Next: once the DB migrations are applied and products are repopulated with `category_id`/`subtype_code`, remove the remaining legacy `product_type` fallback paths from catalogue, SEO, email, and order-lookup helpers.
- 2026-03-21: Added the approved catalogue taxonomy layer to repo migrations without changing app logic: new category and collection tables plus translation tables were introduced, `products` gained nullable `category_id`, `subtype_code`, and `collection_id`, and top-level catalogue categories with KA/EN/RU names and descriptions were seeded in SQL. Why: commerce data is currently empty, so this is the safest point to establish the next catalogue model before repopulating products while still keeping legacy `product_type` intact for the current app. Next: when code migration starts, switch catalogue and PDP logic from `product_type` toward `category_id` plus `subtype_code`, then deprecate direct `product_type` usage later.
- 2026-03-21: Updated the homepage banner so the line under `Levan Margiani` now shows the first active homepage category label, and disappears entirely when no active category exists. Why: the banner should reflect current catalogue content rather than a static `Works` label when categories are available, while staying clean when the catalogue is empty. Next: if the homepage later switches to real product-type categories there too, update this banner source from the same category derivation instead of reintroducing fixed banner copy.
- 2026-03-21: Followed up on the homepage banner credit by increasing its contrast and forcing `G. Margiani` to stay on one line with non-breaking spaces, while keeping the same right-side placement and copy. Why: the first pass placed the requested text correctly but it was still too faint against the banner image and could wrap awkwardly at the name. Next: if the banner art changes later, retune only this small overlay text treatment rather than introducing another badge or background plate.
- 2026-03-21: Added the existing `Art and Tech experience by G. Margiani` credit to the homepage banner’s right side by reusing the shared `footer.designedBy` dictionary key inside the hero overlay. Why: the homepage banner needed that specific credit line visible without introducing new copy or changing the rest of the hero structure. Next: if the banner overlay gets denser later, keep this credit as the right-side secondary detail rather than introducing another separate badge or label row.
- 2026-03-21: Replaced the catalogue page’s static merged filter model with filters and sections derived directly from the current active `productType` values in the loaded catalogue data, so the page now shows `All` plus only real active categories without combining them into presentation buckets. Why: the catalogue filters were still driven by hardcoded `cloths/runners/pillows/scarves` UI groupings instead of the actual active product categories in Supabase. Next: if new commerce product types are activated later, add `catalogue.types.*` translation keys for them so the page can avoid the humanized fallback label.
- 2026-03-21: Replaced the temporary global cart-validation fetch in `CartProvider` with a narrower on-demand `/api/cart/validate` flow used only by the cart and checkout screens, while keeping the earlier homepage/product-gallery empty-state hardening intact. Why: the stale-cart cleanup worked, but the provider-level Supabase read added a networked catalogue concern to a global client context. Next: if cart validation rules expand later, keep them centralized in the route and continue to call them only from surfaces that actually need cart integrity checks.
- 2026-03-20: Hardened the storefront against empty or cleaned commerce data without touching media cards or storage cleanup: the homepage now swaps the empty category carousel for a small fallback message while keeping the existing banner image, the product gallery now shows a proper no-image placeholder instead of an empty frame, and the cart provider now prunes stale local cart items whose products/variants no longer exist in the public catalogue. Why: after commerce data cleanup, the app needed to degrade cleanly on empty products/images and stop carrying browser-local cart entries that could only fail later at checkout. Next: if catalogue public-read rules change later, verify the cart pruning still has read access to active `products` and nested `product_variants`.
- 2026-03-20: Added an in-repo Supabase schema contract for the live V2 commerce tables by introducing versioned migrations for confirmed enum types, catalogue tables, order tables, and `artist_media_cards`, plus a `SCHEMA_CONTRACT.md` that separates confirmed objects from uncertainties. Why: the repo previously had no versioned contract for the active products/orders schema, which made handoff and production verification depend on live database introspection. Next: if exact parity with the live project is needed, inspect Supabase SQL editor output for any non-PK unique constraints, extra indexes, RLS/grant SQL on catalogue/orders tables, and trigger-backed `updated_at` behavior before adding them to the contract.
- 2026-03-19: Upgraded the homepage media rail so cards never render as empty shells when `thumbnail_path` is missing: YouTube cards now derive `i.ytimg.com` thumbnails from the video URL, Facebook posts fall back to a branded Facebook treatment, link/article/exhibition cards render a premium domain-led fallback with favicon accent when available, UI dedupes URLs defensively, and the shuffle still keeps YouTube entries first. Why: the homepage media section needed to stay visually complete and editorial even when Supabase rows do not include thumbnails. Next: if media cards later support richer article metadata, extend the fallback card copy from the same domain/excerpt path instead of reintroducing empty placeholder visuals.
- 2026-03-19: Follow-up deploy fix: included the previously missed [`web/src/lib/emailBids.ts`] mail-link update so bid email code now imports `getPublicBaseUrl` instead of the removed `publicBaseUrl` export. Why: the earlier narrow blocker commit fixed the shared env API but accidentally left this file out, which broke Vercel TypeScript during build. Next: keep narrow commits aligned with all touched call sites whenever shared env exports are renamed.
- 2026-03-19: Hardened the V2 production server/runtime path by switching the internal admin Supabase env shape to a neutral `SUPABASE_ADMIN_KEY` resolved from `SUPABASE_SECRET_KEY` first and `SUPABASE_SERVICE_ROLE_KEY` second, making public-site URL resolution reject localhost fallback on Vercel/runtime environments while still allowing safe local fallback, adding compensating cleanup so failed `order_items` inserts roll back the just-created order row, surfacing a localized generic lookup error instead of collapsing every non-404 tracking failure into “not found”, and syncing the document `lang` attribute to the active locale instead of leaving it permanently `ka`. Why: the remaining blockers were production correctness issues around invalid admin-key selection, bad email links, partial order writes, misleading tracking errors, and wrong document language semantics. Next: once Vercel production confirms the right env path and mail links, reduce the extra public-base-url/admin diagnostics and decide whether to keep the checkout success status badge/UI wording exactly as-is.
- 2026-03-19: Rebuilt the product detail main gallery from a single-image touch/wheel switcher into a horizontal snap track with native overflow scrolling, desktop pointer dragging, and thumbnail/index sync while keeping the existing layout, swatch controls, preview modal, and variant-change image selection behavior intact. Why: the detail page needed to feel like a real carousel on Mac trackpads and desktop, matching the homepage’s naturally swipeable horizontal rails instead of relying on click-only state changes. Next: if the gallery gets arrow controls or pagination later, wire them to the same scroll-synced track instead of reintroducing separate image state logic.
- 2026-03-18: Added the same compact Artiani logo lockup to the V2 track-order page header card so it visually aligns with the branded checkout sidebar treatment. Why: the tracking surface needed the same light brand anchor as checkout without adding heavier decoration or more copy. Next: if more service pages are added later, reuse this small lockup pattern consistently instead of creating a different header treatment for each one.
- 2026-03-18: Refined the V2 checkout layout by removing the extra mid-page help block from the order-success flow and adding a small Artiani logo lockup to the right-side checkout summary panel. Why: the success stack needed one less repeated support element, while the checkout sidebar benefited from a cleaner branded anchor near the order summary. Next: if the checkout sidebar is tuned further later, keep the branding compact and avoid stacking multiple decorative elements above the totals.
- 2026-03-18: Refined the new V2 footer by removing the extra contact label, enlarging the Artiani mark/wordmark, and adding compact mail/phone icons to the clickable contact links. Why: the footer needed a stronger brand lockup and faster contact scanning without adding more copy or visual weight. Next: if the footer grows later, keep the brand and direct contact actions dominant and avoid turning it into a dense sitemap block.
- 2026-03-18: Added a shared V2 footer with the Artiani mark plus clickable email/phone contact info, and reused one small localized contact-help block on checkout, checkout success, and track-order screens. Why: customers need a clear, trustworthy support path around payment, delivery, and order lookup without cluttering the rest of the interface. Next: if a dedicated contact page is added later, keep these footer/help surfaces as the lightweight global access points rather than moving support information entirely into one route.
- 2026-03-18: Normalized the V2 track-order lookup input so order codes are matched case-insensitively and Georgian phone numbers match whether users enter `+995`, `995`, or the local form. Why: tracking should not fail on harmless casing or country-code formatting differences when the underlying order and contact are the same. Next: if more international phone formats are needed later, move this normalization into a small shared helper instead of growing route-local rules.
- 2026-03-18: Switched the V2 server-side Supabase admin env precedence to prefer `SUPABASE_SECRET_KEY` and only fall back to `SUPABASE_SERVICE_ROLE_KEY`, while leaving the public client env path unchanged. Why: Vercel diagnostics confirmed a present but invalid `SUPABASE_SERVICE_ROLE_KEY`, so the server should prioritize the newer secret-key path when both variables may exist during config cleanup. Next: once Vercel is updated to the correct secret key, remove the stale invalid service-role key entirely to keep admin env selection unambiguous.
- 2026-03-18: Added safe Supabase production diagnostics across the V2 public/admin client init paths plus the catalogue, order pricing, order create, and order lookup flows, logging only env presence booleans, chosen key variable names, and sanitized Supabase error details. Why: production on Vercel is failing with `Invalid API key` while localhost works, so the next failing request needs to identify whether the broken path is using the public keys or the admin service key and exactly which env variable name was chosen. Next: after the Vercel logs confirm the broken env path, remove or reduce the extra diagnostics and keep only the useful structured error logging around the affected server routes.
- 2026-03-18: Added safe temporary mail diagnostics to the V2 order-create path by logging mail-env presence booleans, transporter creation, sendMail reach/completion, and sanitized Nodemailer error details, while also returning `emailAttempted` and `emailDebugReason` in the checkout success payload. Why: production order creation was succeeding while email delivery failed silently, so the next production attempt needs to reveal whether Vercel is missing env vars or Gmail SMTP is rejecting the send. Next: once the production root cause is confirmed and fixed in Vercel/Gmail, remove or reduce the extra debug response fields and keep only the safer server logs if still useful.
- 2026-03-18: Added delivery-area details to the V2 order emails by passing `tbilisi/region` through the order creation email payload and rendering a localized delivery-area line in both the customer and admin messages. Why: the internal notification email included contact and address details but was silently dropping whether the order was for Tbilisi or a region, which matters for delivery handling. Next: if admin mail needs more operational detail later, add shipping fee and payment status in the same compact summary block rather than spreading those fields across separate sections.
- 2026-03-18: Fixed the V2 order-status flow end to end by changing new order inserts from legacy `pending` to `awaiting_payment`, adding localized `orderStatus.*` labels in KA/EN/RU, and rendering track results as colored status badges instead of raw enum text. Why: the real creation path in `web/app/api/orders/create/route.ts` was still inserting `pending`, and the tracking UI was exposing raw database values without translation or hierarchy. Next: if admin-side status updates are added later, keep them constrained to this shared status set so the storefront badge mapping and translations stay authoritative.
- 2026-03-18: Shortened the shared V2 order code generator so new orders now use `ART-XXXXXX` instead of the older date-prefixed format, while keeping checkout success and tracking on the stored `order_code` value from the database. Why: the real long-code source was the central `web/src/lib/orderCode.ts` helper used by order creation, so fixing that single generator updates all new orders without any UI-only masking. Next: if legacy long-format orders need coexistence checks later, keep lookup tolerant at the DB value level rather than introducing multiple generators again.
- 2026-03-18: Added a pre-submit payment-process information block to the V2 checkout page with localized guidance about bank transfer instructions, payment confirmation, and delivery timing. Why: customers should understand how payment and delivery work before placing the order, not only after the success screen appears. Next: if the checkout page grows denser later, keep this block near the form and order summary as a compact trust/instruction panel rather than pushing it below the fold.
- 2026-03-18: Redesigned the V2 checkout success/payment page into a clearer post-order flow with a stronger success hierarchy, highlighted order-code block, copy buttons for payment values, clearer next-step instructions, and a more readable order summary. Why: the previous confirmation screen was too flat and hard to scan, especially for bank-transfer instructions that users need immediately after placing the order. Next: if real payment support is added later, keep these confirmation panels and copy interactions as the fallback/manual-payment variant rather than flattening the page again.
- 2026-03-18: Reworked the V2 track flow so the navigation and page naming consistently say “Track order”, the form accepts `order code + email or phone`, and a successful lookup now returns all matching orders for that contact instead of only a single order. Why: the earlier tracking flow was too narrow for repeat customers and still described the feature as a one-order email-only lookup. Next: if users need finer filtering later, add lightweight per-order status chips or a result summary before introducing any heavier account-like history UI.
- 2026-03-18: Tightened the V2 cart item layout on mobile by switching cards to a compact two-column structure with a smaller square thumbnail, reduced padding, and denser typography while keeping the existing desktop layout. Why: the previous mobile cart cards felt oversized and consumed too much vertical space for a simple order-review surface. Next: if the cart still feels long on smaller phones later, consider collapsing secondary option rows before shrinking the primary product information further.
- 2026-03-18: Updated the V2 cart primary CTA copy to say “Proceed to place order” and aligned the same intent in Georgian and Russian. Why: the previous checkout wording felt more generic than the actual action at this stage of the flow. Next: keep the cart and checkout CTA language aligned as the ordering flow wording is refined further.
- 2026-03-18: Updated the V2 product detail print-zone note copy to explicitly mention “see image” in parentheses across all supported languages. Why: users needed a clearer textual cue that the print-area explanation relates to the product visual they are viewing. Next: keep future print-zone copy adjustments in the dictionaries so the note can be refined without touching the component layout.
- 2026-03-18: Added shared V2 add-to-cart feedback across the product detail button and catalogue quick-add button, including a short checked success state, localized “Added” copy, a small inline confirmation on the detail page, and a subtle cart-badge bump in the header. Why: the previous cart action succeeded functionally but did not feel tactile or explicit enough, especially on quick-add actions. Next: if a homepage quick-add button returns later, reuse this same feedback pattern there instead of inventing a third interaction style.
- 2026-03-18: Added horizontal trackpad/two-finger swipe support to the V2 product detail gallery so image changes now respond to wheel-style horizontal gestures in addition to direct touch swipes. Why: the gallery already handled finger swipes on touchscreens, but laptop trackpad gestures were not wired into the image navigation path. Next: if gesture sensitivity needs tuning later, adjust the horizontal threshold and lock timing rather than adding a heavier carousel dependency.
- 2026-03-18: Made V2 cart items clickable by linking each item image and title back to its product detail page while leaving quantity and remove controls unchanged. Why: users should be able to quickly reopen a product from the cart to review details or switch variants without losing their place. Next: if cart browsing needs to feel even faster later, consider adding a compact “open product” affordance near the variant details instead of widening the whole card click target.
- 2026-03-18: Removed the remaining print-area guide overlay from the V2 product detail gallery so oversized cloth sizes are communicated only through the right-panel note. Why: even the softened guide still competed with the product image, and the cleaner gallery better matches the premium image-led direction. Next: keep any future print-zone communication in the textual UI unless there is a clear need for a much lighter image annotation.
- 2026-03-18: Softened the V2 product detail oversized print-area preview by removing the large outer frame/mask effect and replacing it with a subtle inner guide on flat images only. Why: the previous oversized border treatment distracted from the product image instead of quietly supporting the print-zone note in the buy panel. Next: if the preview hint still feels unnecessary later, remove it entirely and rely only on the text note rather than adding a stronger visual treatment again.
- 2026-03-18: Removed the `More works` heading from the randomized product cards block at the bottom of the V2 product detail page. Why: the extra section label felt unnecessary once the cards themselves were visually self-explanatory. Next: if that lower block still feels heavy later, reduce the card count or switch it to a swipe row before adding more labels or framing.
- 2026-03-18: Replaced the V2 product detail page’s previous/next links with a randomized set of four product recommendation cards rendered at the bottom of the page. Why: a small shop-like grid of other works is more useful and visually richer than strict linear navigation between adjacent products. Next: if the recommendation area needs more control later, bias the random set toward the same product family before introducing heavier recommendation logic.
- 2026-03-18: Simplified the V2 cloth print-zone rules so round cloths always use a `110 × 110 cm` print zone, rectangular cloths always use a `110 cm` print width, and the details-panel note now shows width-only wording for rectangular sizes. Why: the earlier mixed fallback logic was harder to reason about and the rectangular note did not need to repeat the full length when only the printable width is constrained. Next: once the final production size set is locked, verify each cloth SKU against these rules and remove any stale numeric print-field values that no longer match the UI logic.
- 2026-03-18: Increased the visibility of the V2 product detail print-zone note by turning it into a highlighted info block with stronger contrast and a bolder printed-size label. Why: the print-area guidance was present but too easy to miss when selecting cloth sizes that have a smaller printable zone than the full fabric. Next: if users still overlook it, consider pairing the note with a compact inline icon or micro-label near the active size button rather than adding more body copy.
- 2026-03-18: Updated the cloth print-area logic so `110x110` variants are always treated as full-print and no longer show the print-zone note or reduced-area preview, even if older numeric print fields still describe a smaller area. Why: the smallest cloth size should not imply an extra unprinted border anywhere in the product detail UI. Next: finish aligning the remaining cloth fallback rules to the latest production sizing guidance before relying fully on the future numeric print-area fields.
- 2026-03-18: Changed the V2 product detail size selector to jump the gallery to the variant `detail` image when a new size is chosen, instead of preserving the previous image index for that specific action. Why: size changes should surface the construction/detail view immediately so the size-specific fabric/finish context is easier to read. Next: if some products need a different preferred image per selector later, promote this into a small configurable image-preference helper rather than branching in the UI.
- 2026-03-18: Added oversized cloth print-area logic to the V2 product detail page with fallback size mapping from current `size_label`, optional support for future numeric `product_variants` size/print columns, a print-area note in the buy panel, and a flat-image preview overlay that shows the natural fabric border when the printable area is smaller than the full cloth. Why: some cloth sizes need a clear distinction between total fabric size and actual printed area, and that should be visible in both text and the design preview without faking the lifestyle imagery. Next: once `width_cm`, `height_cm`, `print_width_cm`, and `print_height_cm` are live in Supabase, keep this path but let the fallback table serve only as a safety net for older rows.
- 2026-03-18: Refined the V2 product detail page with real visual color swatches, touch-swipe gallery navigation, and image-index preservation across color/size changes by moving the gallery state to an index-based flow. Why: the old text-heavy color pills felt clunky, and the gallery reset behavior made variant browsing feel less polished on both mobile and desktop. Next: if more color names appear later, extend the frontend swatch mapping table and keep unknown values on the neutral fallback rather than reverting to text-only buttons.
- 2026-03-18: Moved the enlarged brand/logo badges on homepage media cards back to the top-left corner after testing a centered treatment. Why: the larger centered badges competed too much with the card imagery and fallback panels, while the top-left placement keeps the branding clear without dominating the visual. Next: if the badge still feels heavy later, reduce its size slightly before changing the placement again.
- 2026-03-18: Refined the V2 homepage media cards into a cleaner visual rail by deduplicating rows by URL, removing the footer metadata row, and adding brand-aware fallbacks: YouTube/Facebook badges, favicon badges for other links when available, and large site-name fallback panels when thumbnails or logos are missing. Why: the initial rail worked functionally, but duplicate cards and noisy footer metadata made it feel less editorial, while broken or missing thumbnails needed graceful source-specific fallbacks. Next: if thumbnail quality varies later, add light per-card crop tuning or source-specific art direction instead of reintroducing heavier text metadata.
- 2026-03-18: Adjusted the V2 homepage media rail ordering so all cards always remain in the feed, the full set is shown by default, and `youtube_video` items now get only a probability boost toward earlier positions instead of being hard-grouped at the very front. Why: the rail should feel more naturally mixed while still surfacing video references more often near the lead positions, and the homepage should not silently drop later links once more cards are added. Next: if the distribution still feels off later, tune the YouTube score bias or add per-source balancing without returning to a fixed sequence.
- 2026-03-18: Updated the V2 homepage media rail to randomize card order per request while still promoting `youtube_video` cards to the front, and replaced the mobile-only text buttons with the same compact arrow controls used across breakpoints. Why: the rail needed to feel less fixed and more exploratory, but video references should still stay prominent and mobile browsing should not depend only on swipe gestures. Next: if the media mix grows later, consider adding a light cap per source type before shuffling so one source cannot dominate the rail.
- 2026-03-18: Switched the V2 homepage route to dynamic rendering so Supabase-backed homepage content like `artist_media_cards` is fetched at request time instead of being frozen into the static build. Why: localhost dev showed newly added media cards immediately, but production could miss them because the homepage had been prerendered during build and not regenerated after content changed. Next: if more homepage sections become DB-driven later, keep them on this runtime path or add explicit revalidation rather than relying on fully static generation.
- 2026-03-18: Removed the white edge-fade overlays from the V2 homepage category pager while keeping the arrow controls and touch scrolling. Why: the fade treatment made the shortcut rail feel visually hazier than the rest of the homepage and was no longer needed once the arrows were visible. Next: if the category rail needs more polish later, tune only spacing and arrow prominence rather than reintroducing extra overlay effects.
- 2026-03-18: Updated the new homepage media rail to ignore per-row language filtering so the same curated media list appears on KA, EN, and RU versions of the homepage. Why: this rail is intended to function as one shared editorial feed rather than three separate locale-specific queues. Next: if some cards later truly need locale-only visibility, handle that with an explicit publishing rule instead of the default homepage query.
- 2026-03-18: Replaced the V2 homepage artist biography preview with a new media rail fed by curated `artist_media_cards`, using a lightweight scroll-snap carousel, localized card labels, explicit thumbnails from `thumbnail_path`, and a local fallback image when no thumbnail is provided. Why: the homepage needed a more current, shop-adjacent editorial surface for videos, press, exhibitions, and public links without adding a heavy slider dependency or relying on fragile automatic preview scraping. Next: run `supabase/artist_media_cards.sql`, publish the first curated cards and thumbnails into the `media` bucket, then decide later whether YouTube cards should stay external-only or gain a deferred modal player.
- 2026-03-18: Simplified the V2 catalogue cards further by removing the small product-type label and moving a slightly larger title and price into a tighter stack directly beneath the image. Why: even after the previous spacing pass, the text block still felt too airy and the extra type label diluted the more product-led shop feel the catalogue needs. Next: if card hierarchy still needs adjustment later, tune only the title line clamp and CTA balance before adding any new metadata back.
- 2026-03-18: Added explicit previous/next paging controls to the V2 homepage category carousel so overflowed category cards can be advanced by arrow buttons as well as touch swipe, with the controls appearing only when the row actually overflows. Why: the homepage category strip already worked as a scroll row, but when all cards did not fit in one view it needed a clearer, more shop-like paging affordance on top of swipe. Next: if the homepage top area is tuned further later, keep the carousel controls quiet and utility-focused rather than turning the row into a heavy slider UI.
- 2026-03-18: Finalized the latest V2 catalogue card polish by restoring an explicit “All” category filter, keeping the top filter row horizontally scrollable, and swapping the ambiguous add icon for a clearer cart-plus action while preserving direct add-to-cart from the default variant. Why: after the broader catalogue refresh, the filter row still needed an all-products reset state and the compact CTA needed to read immediately as shopping rather than deletion or utility. Next: if the catalogue needs further tuning later, refine only image crop positioning and hover/tap feedback before introducing any heavier card metadata.
- 2026-03-18: Tightened the V2 catalogue cards further by making the image frame more square, switching card imagery to cropped `object-cover`, simplifying section headers, and replacing the textless plus CTA with a compact bag-style add-to-cart button sized for touch. Why: the first catalogue refresh improved density, but the cards still felt taller than needed and the add action still read more like a generic utility control than a shopping cue. Next: if catalogue imagery still feels uneven later, tune crop positioning per product family before adding more card chrome.
- 2026-03-18: Redesigned the V2 catalogue into a tighter, more image-led collection grid with a cleaner horizontal category bar, more compact section spacing, and smaller shop-style cards that keep the image dominant while adding direct add-to-cart from the default variant. Why: the previous catalogue felt too sparse and editorial for browsing, with tall cards and weak product information density, so the collection view needed to feel more like a premium shop without changing the Supabase-backed product logic. Next: if merchandising needs to evolve later, keep refinements focused on image framing and collection hierarchy rather than adding heavier UI chrome.
- 2026-03-18: Added two lightweight editorial homepage preview sections below the category content: one introducing Artiani as a curated home-object space for Levan Margiani’s work, and one introducing Levan with a short biography preview plus link to the biography page. Why: the homepage needed a bit more trust and context without becoming heavy or dashboard-like, while the fuller biography content can still evolve later on its own page. Next: if the biography page is expanded later, keep the homepage copy short and use it only as a calm preview rather than duplicating the full story.
- 2026-03-18: Refined the V2 homepage top navigation by replacing the temporary filter pills with swipeable image-led category shortcut cards, and changed the hero banner CTA copy from catalogue wording to “Works”/`ნამუშევრები`/`Работы` while keeping links pointed at localized catalogue anchors. Why: the homepage needed a cleaner, more premium category-navigation pattern that feels closer to ecommerce shortcuts without sending users into product pages from the top nav area. Next: if the category shortcut row grows later, keep it horizontally swipeable and sourced from real lead product imagery per category.
- 2026-03-18: Reworked the V2 homepage into a category-led, mobile-first shop surface with a clickable hero banner, horizontal category jump row, per-category product sections, direct add-to-cart cards, and matching catalogue section anchors. Why: the old homepage felt more editorial than shoppable, while the current goal is a cleaner ecommerce-like entry that still fits the museum-minimal visual language and reuses the existing Supabase catalogue data. Next: when paintings are introduced on the homepage later, add them as the first banner/category-led story without changing the current textile catalogue or checkout flows.
- 2026-03-18: Tightened V2 product image selection so catalogue cards now render only a DB `main` image and no longer fall back to other image types, while PDP gallery order continues to follow `main/detail/flat/lifestyle` and then `sort_order` from the database. Why: the previous refactor still allowed non-main fallback in card selection, which could surface the wrong product image first even when storage naming was correct. Next: if any product appears without a catalogue image, add or correct its `main` image row in Supabase rather than adding UI-side fallback heuristics.
- 2026-03-18: Standardized V2 product image handling around one shared type order helper so `main` is the primary live product photo, `detail` follows, `flat` is the design image, and `lifestyle` is an optional later image across catalogue mapping, PDP galleries, and order-item enrichment. Why: image precedence had drifted between product queries and UI, including places that incorrectly preferred `lifestyle` first, which made cards and galleries inconsistent with the intended photo strategy. Next: keep new product image uploads aligned to the `main/detail/flat/lifestyle` contract so cards and PDP galleries stay predictable without per-surface exceptions.
- 2026-03-18: Removed the V2 site header's sticky/mobile reappearing behavior by simplifying `SiteNav` back to a normal in-flow header and dropping the scroll-driven hide/show logic, while leaving page section spacing unchanged because the shared layout and the homepage, catalogue, product, cart, checkout, and track views had no sticky-header compensation offsets to remove. Why: the navigation should scroll away naturally instead of staying pinned or reappearing during scroll. Next: if header spacing is tuned later, adjust it per-page for visual rhythm rather than reintroducing global sticky-header compensation.
- 2026-03-17: Fixed the V2 checkout and tracking routes against the actual live Supabase order schema by remapping `orders` to `order_code/email/phone/note/total_amount`, remapping `order_items` to `product_id/variant_id/unit_price/line_total/snapshot_*`, and keeping order emails optional after successful persistence. Why: the live tables did not match the newer `_cents`/metadata contract in code, which caused checkout to fail before the mail helper was ever reached even though Gmail SMTP itself was valid. Next: document the real Supabase schema in-repo so future V2 flow changes do not drift away from production table names and types again.
- 2026-03-17: Expanded the server-side public URL fallback for email links to also use deployment-style site envs when `PUBLIC_BASE_URL` is not set, and verified the preserved Gmail SMTP config can send a real email. Why: checkout mail sending was already wired, but real order emails should not rely on a localhost link in production and the live SMTP path needed confirmation. Next: set an explicit production `PUBLIC_BASE_URL` (or equivalent site URL env) in deployment so customer track links in real emails always point at the intended public domain.
- 2026-03-17: Restored the V2 order email payload around the preserved mail helpers by keeping checkout email sending optional, and updated the order emails to include current textile option details plus delivery address and note in both customer and admin messages. Why: the checkout flow already tolerated missing mail config, but the email content still needed to reflect the current Supabase-backed V2 order shape instead of older product assumptions. Next: if email formatting needs further polish later, extract the shared SMTP transport and HTML shell without changing the post-order flow.
- 2026-03-17: Fixed the V2 product gallery markup so the hero image button no longer contains the thumbnail buttons. Why: nested `<button>` elements are invalid HTML and were causing a hydration warning/error on the product details page in Next.js. Next: keep interactive gallery controls as sibling elements when layering overlays on top of tappable media.
- 2026-03-17: Removed the visible checkout delivery-fee sentence from the address guidance block, leaving only the address accuracy and timing notes while keeping the shipping selector and total pricing logic intact. Why: the fee information is already communicated more directly through the delivery-area choices and summary totals, so repeating it in the note block added unnecessary copy. Next: if more checkout guidance is needed later, keep it non-redundant with the live pricing UI.
- 2026-03-17: Removed the visible checkout phone-format hint under the phone field while keeping the actual validation behavior in place. Why: the helper copy was no longer wanted in the form UI, but the input still needs the same validation rules during submission and browser validation. Next: if checkout validation messaging needs refinement later, switch to concise inline error states instead of persistent helper text.
- 2026-03-17: Added an explicit checkout delivery-area choice for Tbilisi vs region, moved the delivery guidance block above the address field, and now includes the matching 5/10 GEL shipping fee in the checkout total shown to the customer and saved to `orders.total_cents`. Why: delivery pricing now depends on destination, so the form needs a clear user choice and the visible/order totals must reflect that choice instead of showing only the basket subtotal. Next: if operations need the chosen area stored separately later, add a dedicated order column rather than overloading free-text fields.
- 2026-03-17: Rebuilt the core V2 commerce flow after catalogue: polished the cart with checkout actions, added a Supabase-backed checkout page and order APIs, and replaced the track placeholder with a working order lookup flow using code + email. Why: homepage, catalogue, and PDP were already present, so the next critical step was restoring the actual purchase path on top of the current Supabase textile product model and localStorage cart. Next: once order operations mature, add explicit localized status labels and a documented database contract for the `orders` and `order_items` tables.
- 2026-03-17: Changed catalogue cards to prefer lifestyle images before main images, matching the current product-detail image priority. Why: the browse grid should lead with the more editorial/lifestyle shot when one exists instead of defaulting to the product's technical main image. Next: if needed later, make image priority configurable per product family rather than relying on one shared order.
- 2026-03-17: Polished the V2 catalogue into the main shopping page by rebuilding the catalogue shell and product cards around image-first presentation, elegant filter pills, grouped product families, and localized price/type labels for all active Supabase products. Why: after the homepage, catalogue now needs to function as the primary browse surface rather than a transitional data check, while still preserving the existing Supabase query/model and route structure. Next: if needed later, refine merchandising order or per-family card treatments without changing the shared catalogue/product data flow.
- 2026-03-17: Folded the homepage artist/biography preview into the left `Artiani` hero block and removed the separate standalone biography section below. Why: the homepage should feel tighter and more immediate, with the brand and artist context living in one opening block instead of reading like two separate sections. Next: if the home hero gains more copy later, keep the artist preview compact so the opening panel does not become text-heavy again.
- 2026-03-17: Removed the V2 homepage category-shortcuts block and featured-products block, leaving the hero and artist preview as the primary sections. Why: those two middle sections were no longer wanted on the current homepage direction and removing them tightens the landing page without changing the catalogue/product flows underneath. Next: if merchandising content needs to return later, reintroduce a single focused section rather than stacking multiple browse blocks.
- 2026-03-17: Replaced the V2 home placeholder with the first real Supabase-backed homepage: a premium hero, category shortcuts, featured products, and an artist preview, all localized for EN/KA/RU and built on the current shell/catalogue routes. Why: V2 needed a real storefront landing page that uses live product data without reintroducing hardcoded catalogue content or reworking the broader app architecture. Next: if merchandising needs more control later, add an explicit featured flag or homepage ordering field in Supabase instead of inferring featured picks from the current active product order.
- 2026-03-17: Standardized the Georgian category plural to `თავსაფრები` in the catalogue filters/common labels. Why: the current product terminology should present that family as headscarves in Georgian rather than generic scarves. Next: review whether the singular Georgian `scarf` label should also be renamed for full terminology consistency across PDP and basket surfaces.
- 2026-03-17: Strengthened the empty-basket “Continue shopping” CTA styling so its label keeps a readable white foreground on the cart page. Why: the button text was not visibly rendering in the empty state, which made the main recovery action easy to miss. Next: keep shared CTA contrast explicit anywhere links are styled as buttons instead of relying on inherited text color.
- 2026-03-17: Reworded basket empty-state and basket feedback copy in EN/KA to refer to generic products/items instead of only cloths. Why: the V2 catalogue now includes multiple product families, so cart messaging should not imply that every item is a tablecloth. Next: keep reviewing older basket and catalogue copy for product-family assumptions as more categories are added.
- 2026-03-16: Added the current selected price directly into the PDP add-to-basket button label in parentheses. Why: the primary CTA should confirm the purchase amount at the exact moment of action, especially when color/size choices can affect the selected variant. Next: keep PDP purchase cues tight and avoid duplicating price emphasis elsewhere unless checkout introduces new totals or fees.
- 2026-03-16: Moved the PDP material text below the option selectors so it now reads as the last product note before the add-to-basket CTA, and added mobile-only header hide/show behavior based on scroll direction. Why: the buy panel reads more naturally when the material note supports the final purchase decision, and the compact V2 mobile shell benefits from reclaiming vertical space while scrolling through product content. Next: keep mobile shell behavior subtle and verify the sticky header still feels predictable once more long-form PDP content is added.
- 2026-03-16: Removed the temporary “added to basket” confirmation state from the V2 PDP buy panel, leaving the persistent basket summary as the only add confirmation. Why: once the basket contents are visible on the PDP, the transient success message becomes redundant and noisy. Next: keep basket feedback stable and structural rather than relying on disappearing micro-messages.
- 2026-03-16: Fixed two V2 product presentation issues: the fullscreen image close button now sits above the image overlay on mobile, and catalogue “colors” counts now reflect unique color/style groups instead of raw variant rows across sizes. Why: the close control could be obscured by the preview image, and textile products with multiple sizes were overstating their available colors. Next: keep catalogue summary metrics aligned with how options are actually presented on the PDP.
- 2026-03-16: Tightened the catalogue presentation by making the product card image area use the same white surface as the card body and switching mobile catalogue sections to a denser two-column grid with slightly smaller card content. Why: once full-image `object-contain` was enabled, the darker image well looked disconnected, and one-up mobile cards felt oversized. Next: keep evaluating whether individual product families need their own card aspect or density rules.
- 2026-03-16: Tightened the PDP media/navigation details by enlarging the over-image thumbnail strip and simplifying the neighboring-product labels from “Previous/Next product” to just “Previous/Next”. Why: the overlay thumbs needed a bit more usable size, and the shorter labels keep the navigation cards cleaner. Next: keep PDP helper copy terse and let the product title/image do most of the work.
- 2026-03-16: Added previous/next product navigation back to the V2 PDP as compact cards with arrows, thumbnails, and product labels, derived from the current ordered catalogue list on the server. Why: the PDP now needs a lightweight way to move between neighboring products without returning to the catalogue. Next: keep this neighbor navigation visually compact unless later product families need stronger cross-sell or related-item rules.
- 2026-03-16: Replaced the V2 public Supabase client with a strict `NEXT_PUBLIC_*` configuration that uses `NEXT_PUBLIC_SUPABASE_URL` plus `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` or `NEXT_PUBLIC_SUPABASE_ANON_KEY`, with no fallback to service-role or secret keys. Why: the previous helper could select the wrong key in hosted environments and caused `Invalid API key` failures. Next: keep all browser-safe Supabase reads on this public client and reserve service-role access for server-only helpers.
- 2026-03-16: Switched catalogue cards to show full product images with `object-contain` instead of cropped `object-cover`, while keeping the same card frame and hover behavior. Why: for the current textile products, preserving the full image is more important than filling the card edge to edge. Next: evaluate whether all product families still look balanced with this treatment or need per-type image behavior later.
- 2026-03-16: Expanded the V2 Supabase product-type support from cloth-only to the current supported set (`tablecloth_round`, `tablecloth_square`, `table_runner`, `pillow`, `scarf`) and updated catalogue grouping/labels so all active supported products can flow through the catalogue and PDPs. Why: the query layer was still hard-limiting results to cloth types, which hid valid products like table runners. Next: keep this broader type mapping in sync with Supabase as more product families are introduced.
- 2026-03-16: Extended the minimal V2 basket UI by adding quantity controls on the cart page and showing each line’s product type inside the PDP basket summary. Why: once the basket is visible from the PDP, the cart also needs basic quantity adjustment without leaving the current localStorage model. Next: if needed later, add direct quantity editing or compact line controls to the PDP summary as well.
- 2026-03-16: Added a compact basket summary to the V2 product buy panel so every product detail page can show current basket lines, quantities, and total without leaving the PDP. Why: once add-to-basket works, the PDP should also reflect the basket state nearby. Next: keep this summary compact and later decide whether to add inline quantity controls or keep edits on the cart page only.
- 2026-03-16: Rebuilt the V2 product detail page into a classic ecommerce PDP structure with a dominant left gallery, sticky right buy panel, and lower description/care section, while keeping the existing Supabase product data flow and local basket logic unchanged. Why: the centered experiment weakened hierarchy and did not read like a real product page for home textiles. Next: keep this clear PDP structure while refining selector micro-interactions and later adding checkout work on top of the same basket model.
- 2026-03-16: Replaced the split V2 product detail composition with a centered editorial layout: title/subtitle/price first, then a dominant single image, tight thumbnail row, centered options/actions, and a smaller material/description section below. Why: the previous two-column treatment still felt too form-like and visually segmented. Next: keep future detail-page refinement within this simpler centered hierarchy rather than returning to side-panel layouts.
- 2026-03-16: Redesigned the V2 Supabase-backed product detail layout into a more editorial two-column composition with a larger dominant image, tighter thumbnail row, integrated title/subtitle/material/price stack, and no separate specs card. Why: the previous page worked, but it still felt too form-like and generic for a premium artist shop. Next: keep this visual hierarchy while refining basket and checkout surfaces around the same calmer product-detail layout.
- 2026-03-16: Added the first working V2 basket flow with a minimal localStorage cart (`artiani_cart_v1`), provider-backed header count, add-to-basket action on the Supabase product page, and a simple functional `/[lang]/cart` view showing lines, totals, and remove actions. Also tightened the product media column by pulling thumbnails closer to the hero image and reducing excess vertical space. Why: the V2 catalogue and detail flow needed a usable client-side basket before any checkout work resumes. Next: keep this cart model stable while later wiring checkout/order submission to the same selected variant data.
- 2026-03-16: Added previous/next product navigation to the V2 Supabase-backed detail page, derived from the current ordered catalogue list and rendered as simple localized links at the bottom of the product view. Why: make it easier to move between the current cloth products without returning to the catalogue. Next: keep this server-derived neighbor pattern when more product types are added to the catalogue.
- 2026-03-16: Switched the V2 product detail page price display from the `GEL` code to the lari symbol `₾`. Why: the product UI should use the local currency symbol in the primary price presentation. Next: keep any future cart/checkout V2 price surfaces aligned to the same symbol treatment where appropriate.
- 2026-03-16: Refined the Supabase-backed V2 product detail layout by moving shape/type into a subtitle near the title, removing the generic type row from the right-hand card, making price more prominent, tightening the media column, and enlarging the main image treatment while keeping selectors intact. Why: the initial details page felt too generic and airy for a premium product view. Next: carry the same quieter editorial treatment into later cart wiring and any richer product storytelling blocks.
- 2026-03-16: Reworked the V2 `/[lang]/product/[slug]` page into a real Supabase-backed product detail flow with server-side fetch plus small client-side variant/size selection, image switching per selected variant, and locale fallback for product copy. Why: the first product page implementation was a static default-variant view and did not satisfy real product-detail behavior. Next: keep this source-of-truth model while later wiring cart and order flows onto the same product/variant data.
- 2026-03-16: Updated the V2 catalogue/product UI copy so `tablecloth_square` is presented as Rectangular everywhere user-facing while leaving the DB enum unchanged. Why: the UI taxonomy should read Cloth / Round / Rectangular rather than exposing the old square wording. Next: keep DB-backed taxonomy mapping centralized in the UI layer as more product types are added.
- 2026-03-16: Built the first real V2 Supabase-backed catalogue and product detail flow for `tablecloth_round` and `tablecloth_square`, including locale fallback (`requested -> en -> ka`), filter pills, card grid, and a simple `/[lang]/product/[slug]` page. Why: replace the placeholder catalogue with real DB-backed reads while keeping orders, tracking, env, and mail untouched. Next: add variant selection state, richer product copy/media handling, and later connect cart/order flows to the same product source of truth.
- 2026-03-16: Swapped the mobile shell nav from a hidden menu back to always-visible page buttons, arranged as a compact multi-line grid below the top row. Why: the menu hid too much navigation context, but horizontal scrolling also felt weak on very small screens. Next: keep this visible mobile nav until real page content determines a stronger information architecture.
- 2026-03-16: Replaced the mobile shell’s horizontally scrollable nav pills with a compact menu toggle and dropdown panel while keeping the compact top row and existing desktop nav. Why: the scrollable nav could hide page buttons on very small screens and felt fragile. Next: keep this mobile menu pattern while real content is rebuilt inside the shell.
- 2026-03-16: Tightened the V2 shell mobile UX by making the shared header compact and sticky on small screens, moving language buttons into the top row, switching mobile page navigation to a single-line horizontally scrollable pill row, and reducing placeholder top spacing so headings sit higher. Why: the shell header was too tall and the nav wrapped awkwardly on phones. Next: keep this compact shell while rebuilding real page content and product flows on top of it.
- 2026-03-16: Reset the Next.js storefront to a minimal V2 shell with only locale-aware placeholder pages for `/[lang]`, `/biography`, `/catalogue`, `/cart`, and `/track`; added `ru` dictionaries; replaced deprecated `middleware.ts` with `proxy.ts`; and removed the old storefront UI/routes/data from active usage while preserving env, Supabase, and mail helper infrastructure. Why: establish a clean rebuild foundation without exposing secrets or breaking deployability. Next: define the new domain model and reintroduce catalogue, product, cart, and order flows incrementally on top of this shell.
- 2026-03-16: Added a Supabase-backed catalogue/product read path for `tablecloth_round` and `tablecloth_square` without removing the old hardcoded product path. Why: start the storefront migration to database-backed product reads while keeping checkout/order/tracking logic untouched and preserving a fallback for non-migrated slugs. Next: add anon/public Supabase envs, migrate cart/order pricing to DB-backed variants, and expand the same query layer to the homepage and remaining product families.
- 2026-03-16: Added `PROJECT_AUDIT.md`, `ARCHITECTURE_MAP.md`, and `RUNNING_THE_PROJECT.md` after a read-first repository audit. Why: provide a concrete engineering handoff covering stack, structure, runtime flows, setup, risks, and next steps without changing application code. Next: add `.env.example`, document Supabase schema/migrations, and replace deprecated `middleware.ts` with the current Next.js `proxy` convention.
- 2026-02-24: Initialized Phase 0. Updated AGENTS.md and created repo skills + audit checklist in progress.
- 2026-02-24: Added repo skills under .agents/skills and refreshed AGENTS.md per new workflow.
- 2026-02-25: Removed OTP/Twilio verification artifacts (deleted OTP/lookup/bid API routes, removed track page/component/lib, removed OTP auction + track i18n keys, and removed Twilio env notes from AGENTS.md), and fixed codex-workloop SKILL frontmatter YAML quoting. Why: roll back SMS verification and restore pre-OTP behavior. Next: if tracking/bid verification is needed later, reintroduce with a non-Twilio flow behind a dedicated feature branch.

## Phase 1 Audit (sequential — sub-agents not available)

### Prioritized checklist
1) i18n-sweep: move remaining user-facing literals into EN/KA dictionaries
- Why: Requirement: no hardcoded UI copy.
- Files: web/app/[lang]/layout.tsx, web/app/[lang]/product/[slug]/page.tsx, web/src/components/CheckoutForm.tsx, web/src/components/ProductDetails.tsx, web/src/components/CartView.tsx, web/src/i18n/en.json, web/src/i18n/ka.json.
- Risk: Low (string-only changes).
- Test/Verify steps: Run `npm run lint` and `npm run build`. Check `/ka/product/*` and `/en/product/*` titles, checkout confirmation text, cart summary lines.

2) Remove unused i18n keys + stale “campaign” copy where still referenced
- Why: Reduce leftovers / context bloat; align copy with current museum-minimal direction.
- Files: web/src/i18n/en.json, web/src/i18n/ka.json; confirm usage in web/src/components/ProductDetails.tsx and cart/checkout.
- Risk: Low (copy cleanup).
- Test/Verify steps: Lint/build; spot-check product details empty-options text and cart empty subtitle.

3) Cleanup unused imports / minor leftovers
- Why: Reduce noise and keep diffs clean.
- Files: web/src/components/ShopCatalog.tsx (unused import), any unused helpers found by lint.
- Risk: Low.
- Test/Verify steps: `npm run lint` clean.

4) UI component dedupe (optional, small)
- Why: Reduce repeated button/chip markup without changing layout.
- Files: web/src/components/*, possibly add `web/src/components/ui/*`.
- Risk: Medium (could shift layout if careless).
- Test/Verify steps: Lint/build; visually compare buttons/chips across Home, Product, Cart, Checkout.

5) Next.js sanity pass
- Why: Catch App Router pitfalls (metadata, async params, Link scroll).
- Files: web/app/[lang]/*, web/src/components/ProductDetails.tsx, web/src/components/ShopCatalog.tsx.
- Risk: Low to medium.
- Test/Verify steps: Run dev server, open `/ka/catalogue`, `/ka/product/[slug]`, `/en/product/[slug]` and verify scroll + titles.
- 2026-02-24: Item #1 i18n-sweep: moved remaining UI literals to EN/KA (site title, separators, qty labels, bank fields) and wired usages. Verify: `npm run lint`; `npm run build` (fails to fetch Google fonts offline).
- 2026-02-24: Item #2 cleanup: removed unused i18n keys (home.hero.kicker, shop.kicker/subtitle/card_view, product.personalization_subtitle/campaign_price) and cleaned “campaign” copy. Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Item #3 cleanup: fixed ShopCatalog missing useState import (runtime error) and kept imports tidy. Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Item #4 ui-components: added reusable Chip and applied to catalogue filters + cards toggles; suppressed cart count hydration mismatch. Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Item #5 nextjs-sanity: fixed type cast in ShopCatalog filter labels (kind vs type). Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Disabled Google Fonts fetch by removing next/font usage and setting serif font stack in globals. Verify: `npm run lint`; `npm run build` fails in sandbox (Turbopack PostCSS process permission).
- 2026-02-24: Fixed build type error by widening layout params to string and normalizing via isLocale/defaultLocale. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Fixed cart count hydration by loading localStorage on mount (CartProvider) and keeping save guard. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Adjusted cart hydration to use useLayoutEffect so header count updates before paint. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Updated header layout (nav links next to brand + active state) and enlarged PDP back button. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Added underline active state for header nav links. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Centered brand and stacked nav under it on mobile in SiteNav. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Removed the per-item "each" suffix from cart line-item prices so rows show only the price value. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Fixed cart reset on language switch by skipping the first post-hydration cart persistence write in CartProvider. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added checkout-style phone country picker to painting auction bid form (country code + local number input). Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added minimal server-only infra for Supabase + transactional email (`env.server.ts` validation and memoized `getSupabaseAdmin`) with required env checks including `ORDERS_ADMIN_EMAIL`. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added server-side order pricing recomputation (`orderPricing.ts`) and Node-crypto order code generation with unique-constraint retry helper (`orderCode.ts`) for future order API use. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Implemented checkout order creation flow: Node runtime API route inserts `orders` + `order_items` in Supabase with server-side repricing and unique code generation, sends customer/admin transactional emails, and CheckoutForm now submits to API with success/error states. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added order tracking by code+email: Node runtime lookup API (`/api/orders/lookup`) and new `/[lang]/track` page with localized form/results (status, created date, items, total) and generic not-found handling. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added localized track-order link to header navigation (`SiteNav`) with the same nav link styling and locale-preserving route. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Split server env config into required `envSupabase` and optional `envEmail`; order creation now proceeds without email env and returns `emailSent: false` when email config is missing. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Updated tracking UX: `track.linkLabel` now reads “Track order” and order lookup now returns all orders for an email once a valid `code+email` pair is provided, with track page rendering all matched orders. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Switched transactional email transport to Gmail SMTP via Nodemailer; env now uses optional `envMail` (`GMAIL_USER`, `GMAIL_APP_PASSWORD`, `ORDERS_FROM_EMAIL`, `ORDERS_ADMIN_EMAIL`) while order creation remains successful when email sending is unavailable. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Localized order email templates by order language, fixed email track links to absolute `${PUBLIC_BASE_URL}/${lang}/track` (fallback `http://localhost:3000`), and added reusable localized payment instruction blocks (TBC/BOG IBAN + transfer reference) via `paymentInstructions.ts`. Why: ensure checkout emails are language-correct and include actionable tracking/payment details. Next: replace placeholder IBAN/account values with final production banking details. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Enhanced order email line-item details to include product kind and selected options (signed/custom text/postcard or greeting card back), so customers/admin can see exactly what was ordered in each line. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added localized bank transfer block to checkout success state (amount, reference/order code, account name, TBC + BOG IBANs, and transfer notes) directly below order confirmation code. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Implemented real auction bid flow: added Node API route `/api/bids/create` with validation, Supabase persistence in `bids`, BID code generation with unique retry, and non-blocking bidder/admin SMTP emails (`emailBids.ts`) including localized copy, absolute track link, and auction deposit instructions via `paymentInstructions` variant. Updated painting bid form to submit to API (required email field), show bid success code/email status, and keep localized generic error handling. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Changed painting bid UX to redirect after successful submit to dedicated localized result page (`/[lang]/bid`) instead of rendering inline; page shows bid code, email status, deposit transfer block (reference + TBC/BOG IBANs), and tracking link. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Fixed bid-result translation flow by preserving query params during language switch in `SiteNav` (so bid code/details survive locale toggle), and added painting details on bid result page (painting title/slug + bid amount from submit context). Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Expanded tracking lookup so a valid `email + one code` match can be either order or bid code; API now returns both all orders and all auction bids for that email, and track UI renders both sections. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Disabled interactive form fields during submission across checkout, tracking lookup, and painting bid forms (inputs/selects/textareas/buttons) to prevent duplicate submits and in-flight edits. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Fixed hydration mismatch from currency formatting by replacing `Intl.NumberFormat` in `money.ts` with deterministic server/client-safe grouping logic, so SSR and browser render identical GEL strings. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Hardened bid amount handling for large values: precise cents parsing (up to 2 decimals), explicit upper bound validation, and out-of-range DB insert errors now return 400 (validation) instead of 500. Bid input now includes `step=0.01` and a max aligned with cents bound. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Updated auction bid range to enforce `minimum bid ... 2,000,111` GEL consistently in both client input (`max`) and backend validation. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Refactored Home into a new server-rendered `HomePage` component with a paintings-first, product-led layout: hero with featured painting + thumbnails, featured originals grid, gifts category tiles, curated bundle collages, compact museum-label bio, and final CTA strip. Home now uses product images from `products.ts`, shows no prices, and removes the previous instructions block. Added new localized `home.*` keys in EN/KA with parity. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home follow-up polish: reduced hero painting visual size, added a visible hero auction badge, updated hero subtitle copy to begin with paintings emphasis, moved painting grid lower so section 2 is non-painting, and made gifts/bundle tiles fully clickable (including category-filter links like `catalogue?type=calendars`). Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home follow-up cleanup: removed the bottom “Featured originals” block and moved the compact Levan bio block into the hero area directly under CTA buttons to use top-section whitespace better. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home microcopy/UI tweak: removed per-card “დათვალიერება/Explore” labels from gifts and bundle tiles and replaced with subtle `>` affordance while keeping the whole tile clickable. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home finalization polish: kept catalogue `?type=` links (confirmed ShopCatalog query filtering path works), removed static hero bid-count text while keeping AUCTION badges, and adjusted hero spacing/details per latest UX feedback. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Redesigned product detail UX with a split component architecture: interactive gallery with lightbox (`ProductGallery` + `ProductLightbox`), sticky purchase panel (`ProductPurchasePanel`) preserving existing cart/auction logic, structured Story/Details/Delivery info sections (`ProductInfoSections`), and server-rendered related items grid (`RelatedProducts`). Added required localized product UI keys (EN/KA parity) and kept layout museum-minimal with desktop two-column flow and mobile sticky action bar. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: UI polish pass for mobile nav + product detail UX: rebuilt `SiteNav` mobile behavior with compact header (logo left, cart + menu right) and locale-preserving drawer links (Home/Catalogue/Cart/Track/About + language toggle), tightened product gallery thumbnails with subtler selected state, reduced hero artwork padding, and softened purchase/auction panel visual weight while keeping sticky mobile CTA behavior and existing cart/bid flows unchanged. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Removed custom text personalization (`addText`) across product, cart, checkout, pricing, and order email flows; only signature and card back variant options remain active. Updated cart item ID/options shape and removed unused i18n keys related to custom text. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Removed the Product Details "Story" info card and reflowed info layout to Details + Delivery only, constrained to the left content column (`lg:col-start-1`) so these cards no longer span beneath/intersect the sticky purchase panel column. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Simplified product-page prev/next navigation: removed the large slab card block, added subtle previous/next arrow buttons over the hero gallery image, and added a small editorial "next work" link below related works. Navigation now prefers same-kind sequence and falls back to global `products.ts` order at edges. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Replaced placeholder painting catalogue entries with three real auction listings (`shoba2`, `shoba`, `angelozi`) in `products.ts`, including localized names/summary/description and Supabase Storage image URLs with auction metadata (min bid + end date). Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Replaced placeholder cards listings with real `memories-cards` data in `products.ts` (localized copy + Supabase postcard/greeting images), kept card personalization without `addText` (signature remains), and set pricing so greeting back adds +10 GEL on top of postcard base while signature surcharge still applies. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Updated painting image rendering to avoid cropping in product/gallery and homepage contexts (contain fit with paper-tone frames), adjusted painting prev/next nav thumbs to avoid crop, and restyled AUCTION badges to high-contrast top-left pills with shadow for better readability in detail and catalogue/home cards. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added 4 real bookmark products (bundle + 3 singles) with Supabase images. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added 3 real calendar products (bundle + wall + desk) with Supabase images. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added print size variants support (UI + server pricing). Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Home update: surfaced a compact above-the-fold quick-shop row (cards/bookmarks/calendars product tiles) under hero CTAs and removed the Curated Sets/Bundles section while keeping paintings featured. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added 3 posters + 1 poster bundle with size variants. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Fixed metadata description i18n key mismatch in `app/[lang]/layout.tsx` by switching from missing `home.subtitle` to existing `home.hero.subtitle`, preventing raw key fallback in page metadata. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Updated Levan Margiani biography content in EN/KA across About page sections (overview, practice, selected milestones) and Home bio snippet with link to About, keeping museum-minimal tone and bilingual parity. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Made Home quick-shop tiles mobile-safe by using responsive grid columns (1/2/3 across breakpoints) and preventing tile min-width overflow, removing horizontal page scroll caused by the quick tiles section. Verify: `npm run lint`, `npm run build`.
- 2026-03-19: Enriched homepage YouTube media cards server-side by deriving the video ID, using the canonical `i.ytimg.com` thumbnail, and fetching the real title through YouTube oEmbed with memoized fallback handling. If the remote title fetch fails, cards now keep a meaningful DB title or fall back to `YouTube Video` without breaking builds/runtime. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-19: Updated shared artist social links to use brand-colored Facebook and Instagram icons in every placement, and shortened the social section label to `Watch` / `ნახეთ` / `Смотрите` across EN/KA/RU. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-19: Refined the footer layout to a tighter three-column balance on desktop (brand, contact, socials) and a cleaner stacked order on mobile, with quieter icon-only social links in the footer so the contact column stays primary. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-19: Updated the Georgian social label from `ნახეთ` to `ადევნეთ თვალი` so the shared artist-links row uses the requested phrasing everywhere it appears. Verify: dictionary-only text change.
- 2026-03-19: Added a focused SEO enrichment pass across Artiani V2: localized page metadata for home/catalogue/product/checkout/track, richer product/detail image alt text, homepage Organization JSON-LD, product ProductGroup JSON-LD with variant offers, and stronger on-page copy for Artiani, Levan Margiani, Georgian design, and textile categories without adding keyword meta tags or stuffing. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-19: Added the requested `designed by g. margiani` footer credit as a small localized footer note under the contact block without changing the footer structure or visual hierarchy. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-19: Refined the footer into a tighter two-part composition with a more intentional brand block, a compact contact/social cluster, and an updated closing credit line reading `Art direction by G. Margiani`. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-21: Catalogue page now keeps taxonomy/category order intact while randomizing product card order within each category section, including filtered category views. This only changes per-category card ordering and leaves filters/headings untouched. Verify: `cd web && npm run lint`, `cd web && npm run build`.
- 2026-03-22: Added a one-off Node storage-maintenance script at `web/scripts/rename-product-storage-files.mjs` that loads `web/.env.local`, defaults to dry-run, and renames the requested `products` bucket objects through Supabase Storage `move()` instead of SQL; dry-run showed 8/8 ready and execute moved all 8 successfully. Why: the affected product image paths needed a safe server-authenticated storage rename with per-file success/failure output and no app or schema changes. Next: verify any catalogue or seeded data that references these filenames now resolves the renamed storage objects as expected.
- 2026-03-22: Adjusted catalogue card ordering so round tablecloths are shown before rectangular/other tablecloth entries within the catalogue page while preserving the existing per-category randomization for the rest of the cards. Why: rounded cloths need to lead the tablecloth section without changing category grouping or broader catalogue layout. Next: if a stricter merchandising order is needed later, move this priority logic into a shared catalogue sorter instead of keeping it page-local.
- 2026-03-22: Updated product-detail variant switching so changing color or size no longer forces the gallery onto a preferred detail shot; the gallery now preserves the currently viewed slot by image type when possible, so main stays on main and detail stays on detail across variant changes. Why: the automatic detail-image jump was overriding the user’s current view while browsing variants. Next: if variants later gain more than main/detail image roles, extend the carry-over logic to preserve additional named image types explicitly.
- 2026-03-22: Split `table_runner` products into two shared catalogue display groups on category-list surfaces only, using existing `subtype_code` values so `small` runners render under `რანერები` and `large` runners under `გრძელი რანერები`, while the real category and PDP logic stay unchanged. Why: catalogue/home grouping needed separate runner buckets without changing Supabase taxonomy or creating new top-level categories. Next: if merchandising rules expand further, keep extending the shared catalogue display-group helper rather than adding per-surface grouping logic.
- 2026-03-22: Added a compact localized illustration note to the PDP buy/info column so product pages now state that images are illustrative and actual products may vary slightly in color, ornament detail, and size, without changing layout or affecting other pages. Why: the requested expectation-setting copy belongs near the buy/info section and needed EN/KA/RU parity through i18n keys rather than hardcoded component text. Next: if more PDP microcopy is added later, keep it in the same calm info treatment instead of introducing louder warning styling.
- 2026-03-22: Moved the PDP illustration note from the right buy/info panel to directly beneath the gallery and updated the EN/KA/RU copy to point shoppers toward detail photos for a truer view of the design, while keeping the note styling quiet and compact. Why: the note belongs with the image-reading flow rather than the purchase controls, and the revised wording better sets expectations about illustration vs. detail shots. Next: if more gallery-adjacent guidance is added later, keep it grouped under the image block rather than reintroducing copy into the buy panel.
- 2026-03-22: Added a narrow PDP/cart/order option pass without any Supabase or schema changes: runner products now expose a material selector backed by existing real `canvas`/`velvet` variants on the product page, while pillow products now expose a UI-only print-side selector (`one-sided` / `both-sided`) that keeps the same images, adds +10 GEL for both-sided pricing, persists in cart identity/totals, and is carried into order snapshots through existing option text fields. Why: runners needed their real material variants surfaced directly on PDP, and pillows needed a purchasable print-side option without duplicating DB variants or changing taxonomy. Next: if more UI-only purchase options are introduced later, keep them flowing through the same cart/order snapshot pattern unless a real schema-backed option model becomes necessary.
- 2026-03-22: Simplified the pillow print-side selector copy on PDP so the button now reads only `both-sided` / `ორმხრივი` / `Двусторонняя`, while the +10 GEL surcharge behavior itself remains unchanged in displayed/cart/checkout pricing. Why: the surcharge should affect totals, but the selector label itself should stay cleaner and less salesy. Next: if pricing hints are needed later, place them near the total or as quiet helper text instead of embedding them in the option button label.
- 2026-03-22: Raised PDP option-selector readability for accessibility: selectable groups now use clearer body-sized headings, larger tap-friendly chips, and stronger selected-state contrast, and the material/print-side labels now read `Choose material` / `Choose print side` with matching KA/RU copy. Why: important purchase choices should not rely on tiny, faint labels or subtle chips, especially for older users. Next: if similar selector patterns appear on other pages later, reuse this clearer treatment instead of reintroducing the older faint uppercase style.
- 2026-03-22: Further strengthened PDP selector affordance for older users by increasing chip height/spacing, adding clearer resting-state borders and shadows, preserving readable text weight, and making the selected state more visually obvious while keeping the same layout and option structure. Why: the selectors should feel plainly interactive and easy to tap, not merely decorative. Next: if PDP usability testing continues, check whether color/size selectors need the same treatment on any future custom option groups.
- 2026-03-22: Refined the PDP selector labels and sizing again: color now uses a clear `Choose color` heading, size uses `Choose size` only when multiple sizes are available, and the option chips were scaled down from the previous pass so they remain readable and tappable without feeling oversized. Why: the option groups need explicit guidance, but the chips should still feel elegant and proportionate. Next: if more PDP usability feedback comes in, keep tuning selector density before introducing any structural layout changes.
- 2026-03-23: Refactored the shared site header/navigation in a narrow pass: desktop now keeps visible page links while the right side carries a click-open language switcher (flag + code), a new profile button, and the cart button; mobile now keeps language/profile/cart in the header and moves page links into a burger menu panel. The profile button currently routes to the existing `/track` page as the safest personal/order destination because no dedicated profile/account route exists in the repo. Why: the header needed clearer right-side actions, a less cluttered language control, and better mobile usability without redesigning the broader site shell. Next: if a real account/profile route is added later, repoint the profile button there without changing the header interaction model.
- 2026-03-23: Header follow-up polish: removed the circular button chrome from the header action icons for a lighter look, and moved the mobile burger control to the left side of the header while keeping the same menu behavior and right-side language/profile/cart actions. Why: the icon actions should feel cleaner, and the mobile menu trigger belongs on the left for quicker orientation. Next: if header refinement continues, tune spacing/weights before changing the broader shell structure.
- 2026-03-23: Header micro-polish follow-up: slightly enlarged the profile/cart icons, removed the framed button treatment from the language control, and centered the Artiani logo block horizontally on mobile while leaving desktop alignment unchanged. Why: the actions needed a bit more visual presence, and the mobile header reads more clearly with the brand centered between left menu and right actions. Next: if header tuning continues, validate the mobile balance against longer locale labels before altering structure again.
- 2026-03-23: Nudged the header cart-count badge closer to the cart icon so the relationship reads more clearly without changing the icon or badge size. Why: the previous offset made the count feel visually detached from the cart action. Next: if more icon refinements are needed, keep them to positioning and spacing before changing the underlying header structure.
- 2026-03-23: Fine-tuned the cart-count badge position again by moving it slightly farther outward from the cart icon, preserving the closer association while giving the badge a bit more breathing room. Why: the previous badge pass was slightly too tucked into the icon silhouette. Next: if more badge tuning is needed, adjust only offsets rather than changing badge scale or color.
- 2026-03-23: Simplified the header language trigger further by removing the locale text and dropdown arrow, leaving only a slightly larger flag icon as the visible control while preserving the same click-open language menu behavior. Why: the language action reads more cleanly as a compact symbol in the right-side control cluster. Next: if more header simplification is needed, keep trimming visible chrome before changing menu behavior.
- 2026-03-23: Adjusted the mobile header layout so the Artiani logo block now sits back next to the burger menu with spacing, instead of being centered independently. Why: the mobile header reads more naturally when the navigation trigger and brand sit together on the left. Next: if mobile header tuning continues, keep refining spacing before changing icon or logo scale again.
- 2026-03-23: Fixed header menu layering on content-heavy pages by moving the mobile burger interaction to a real slide-out side drawer with backdrop and by promoting the language chooser to a higher-level floating panel, so both menus now open above page content reliably. Why: the previous local popups could be visually covered by homepage sections and did not feel strong enough as navigation surfaces. Next: if header polish continues, keep drawer/panel behavior stable and focus on spacing or copy rather than changing interaction patterns again.
- 2026-03-23: Tightened the header menu fix further: the mobile burger now uses a more solid, full-height drawer surface with stronger shadow/backdrop contrast, and the language dropdown is anchored directly to the language button again instead of opening from a detached page-level position. Why: the drawer needed to feel like a real panel rather than blending into homepage imagery, and the language menu needed spatial continuity with its trigger. Next: if header tuning continues, keep the current drawer/dropdown positioning model and adjust only spacing or surface tone.
- 2026-03-23: Finalized the mobile drawer layering fix by rendering the burger drawer/backdrop through a portal to `document.body` with explicit high z-index separation, so homepage hero/cards can no longer paint above it or clip it through parent stacking contexts. The drawer remains a fixed left panel with a solid light background, and the backdrop stays beneath it but above page content. Why: viewport-level rendering is the most reliable fix when page sections create competing stacking contexts. Next: keep the drawer on the portal path for future mobile-nav changes instead of moving it back inside header markup.
- 2026-03-23: Updated the mobile header control order so the burger trigger now sits on the right beside profile/cart, and moved language switching into the mobile drawer instead of keeping a separate mobile header button. Desktop language behavior stays unchanged. Why: the mobile header is cleaner when all secondary actions live on the right and locale switching is grouped with the rest of the mobile navigation surface. Next: if more mobile-nav cleanup is needed, keep it inside the drawer rather than adding more top-bar controls.
- 2026-03-23: Follow-up mobile header adjustment: kept the burger trigger on the right and fully moved mobile locale switching into the drawer content, while leaving the desktop language dropdown untouched. Why: this keeps the top bar less crowded on mobile and makes the drawer the single place for navigation-related choices. Next: if further mobile header cleanup is needed, continue consolidating controls inside the drawer rather than splitting them back into the top bar.
- 2026-03-23: Simplified the desktop header navigation styling by removing the rounded black/grey pill treatment from the top nav links and centering the nav group horizontally across the desktop header. Active state now reads through text emphasis and a subtle underline rather than a filled button. Why: the desktop header should feel lighter and more editorial while keeping the current layout concept intact. Next: if more header refinement is needed, keep desktop nav changes typographic rather than reintroducing heavier button chrome.
- 2026-03-23: Updated the shared header nav labels so the separate `Biography` item is removed from the main menu and replaced with `About Artiani` after `Catalogue`, while still pointing to the existing biography/about route in the repo. Why: the top navigation should frame that page as brand/context information rather than artist biography only. Next: if a dedicated About Artiani route is added later, repoint this nav item there without changing the menu structure.
- 2026-03-23: Aligned the mobile drawer direction with the trigger position by changing the burger panel to slide/open from the right side instead of the left. Why: with the burger icon on the right, the drawer should emerge from the same side to feel spatially consistent. Next: if mobile drawer polish continues, keep the right-side entry direction and tune only spacing or animation.
- 2026-03-23: Simplified the mobile drawer styling by removing the filled black/grey button treatment from the drawer nav items and changing the language selector to a single top row of flags without text. Why: the drawer should feel lighter and less button-heavy while still keeping locale switching easy to spot at the top. Next: if more drawer refinement is needed, keep it typographic and spacing-based instead of reintroducing heavier filled controls.
- 2026-03-23: Expanded the mobile drawer content in a narrow pass: removed the visible `Menu` heading, added in-drawer shortcuts for profile and cart, and reused the footer’s contact/social/footer-note information at the bottom of the drawer. Why: the mobile drawer should function as a fuller navigation surface without forcing users back to the footer for key contact/context details. Next: if more drawer cleanup is needed, keep reusing existing footer/shared content rather than adding new one-off mobile-only copy.
- 2026-03-23: Refined the mobile drawer information architecture again: centered the locale flags on one row at the top, arranged the main shortcuts as a vertical list (`Profile`, `Cart`, `Home`, `About Artiani`), added a separate list of catalogue category links using the existing `?type=` filters, and simplified the bottom contact block by removing the `Contact` heading while adding mail/phone icons. Why: the drawer needed a clearer hierarchy between shortcuts, categories, and footer info without adding new pages or changing core behavior. Next: if drawer polish continues, keep improving hierarchy and spacing rather than adding more sections.
- 2026-03-23: Cleaned up the mobile drawer text alignment and category copy: only the top flag row remains centered, text links are left-aligned again, the divider after the primary shortcut list was removed, and the category entries now use proper translated labels instead of raw i18n keys. Why: the drawer should read like a normal navigation list, and untranslated key strings are not acceptable in the UI. Next: if more drawer copy changes are needed, keep adding explicit i18n keys rather than falling back to internal key names.
- 2026-03-23: Adjusted the mobile drawer order so `About Artiani` now sits below the category links instead of in the primary shortcut group. Why: the drawer reads more clearly when the first block is reserved for direct utility shortcuts and the brand/about page sits with broader discovery links. Next: if more drawer IA tweaks are needed, keep refining ordering rather than adding more visual separators.
- 2026-03-23: Refined the mobile drawer hierarchy again by moving `Main` to the top of the primary shortcut list and removing the divider between the category list and the trailing `About Artiani` link. Why: the drawer should start with the most universal navigation target and keep the lower link stack visually lighter. Next: if more drawer cleanup is needed, keep reducing unnecessary separators before changing the content set.
- 2026-03-23: Removed the underline treatment from the mobile drawer’s top shortcut list, so entries like `Basket` read as plain text links rather than tab-like marked items. Why: the shortcut block should stay cleaner and less emphasized than the main header nav. Next: if more drawer link styling tweaks are needed, keep the shortcut section typographic and avoid reintroducing extra decoration.
- 2026-03-23: Removed the remaining divider under the mobile drawer’s top shortcut block, so the area beneath `Basket` no longer carries an extra rule. Why: that separator was still making the shortcut stack feel heavier than intended. Next: if more drawer cleanup is needed, continue removing redundant separators before changing spacing.
- 2026-03-23: Restored active-page underlines inside the mobile drawer by marking the current top shortcut and the current catalogue category filter, using the live `?type=` query on catalogue pages so burger-menu navigation now clearly shows where the user is. Why: the drawer needed the same current-page clarity as the main nav without adding heavier styling. Next: if drawer navigation expands further, keep active-state logic centralized in the shared header instead of scattering page-specific checks.
- 2026-03-23: Added a dedicated `Catalogue` link to the mobile drawer’s primary shortcut list directly under `Basket`, and indented the category links slightly beneath it to make the hierarchy read as catalogue sub-navigation. Why: the drawer needed a clearer parent-child relationship between the general catalogue entry point and the specific category filters. Next: if the drawer structure changes again, keep the catalogue block grouped rather than mixing category filters into the top utility shortcuts.
- 2026-03-23: Changed category navigation from the homepage carousel and mobile drawer to use catalogue section anchors instead of `?type=` filters, so these entry points now open the full catalogue and smoothly scroll to the matching section while the catalogue page’s own filter pills remain unchanged. Why: landing on a category from discovery/navigation should guide users to the right spot without collapsing the catalogue into a filtered view. Next: if more entry points should behave the same way, reuse the shared catalogue section-link helper instead of reintroducing raw filter URLs.
- 2026-03-23: Hardened the shared footer for small and awkward in-between widths by keeping its brand, social, and contact blocks stacked longer, allowing the social/contact rows to wrap naturally, and only returning to a three-column layout once there is enough horizontal room. Why: the previous footer grid moved into a crowded horizontal layout too early, which caused collisions between the brand block, social handles, and contact links. Next: if footer copy grows later, keep new items inside the existing wrapping rows instead of forcing more fixed-width columns.
- 2026-03-23: Reordered the mobile drawer footer block so the Facebook and Instagram links now appear above the email and phone rows. Why: inside the burger menu, the social links should lead the footer-style info stack before the direct contact lines. Next: if the drawer footer gets any more refinements, keep them limited to content order and spacing rather than introducing a separate mobile-only design.
- 2026-03-23: Moved the mobile drawer’s language flags onto the same top row as the close button. Why: this keeps the drawer header tighter and makes both the locale controls and close action easier to scan in one place. Next: if the drawer header changes again, keep the top row limited to utility controls rather than adding extra labels.
- 2026-03-23: Rebuilt the shared footer into three explicit sections: a top brand row, a middle social/contact area with independent wrapping behavior, and a separated bottom signature row with the seal icon and credit. Why: the earlier footer kept different content types negotiating the same horizontal space, so awkward widths produced different collisions instead of one stable responsive system. Next: if footer content expands later, place new items inside one of these existing sections rather than reintroducing mixed-purpose rows.
- 2026-03-23: Simplified the footer again by removing the top Artiani brand row and flattening the four remaining social/contact links into one evenly spaced responsive grid above the signature row. Why: with the brand row removed, the footer reads more calmly and the four link items can share space consistently instead of splitting into uneven sub-blocks. Next: if footer hierarchy changes again, keep the upper content as one explicit grid rather than reintroducing separate competing link groups.
- 2026-03-23: Refined the simplified footer links into two explicit columns: Facebook/Instagram stacked on the left and email/phone stacked on the right, while still collapsing cleanly on smaller screens. Why: the footer reads more predictably when social and direct contact stay grouped instead of appearing as four equal peers. Next: if the footer link area changes again, keep these two vertical groups intact rather than mixing social and contact items across the same column.
- 2026-03-23: Kept the footer signature row on one horizontal line at all sizes, so the seal icon and `Art and Tech experience by G. Margiani` now stay together instead of stacking on small screens. Why: that closing line should read as one calm signature unit rather than breaking into two separate rows. Next: if the signature row is adjusted further, keep its horizontal composition intact.
- 2026-03-23: Added icons to the mobile drawer’s page links (`Main`, `Profile`, `Basket`, `Catalogue`, `About Artiani`) while keeping the category list text-only. Why: the top-level navigation benefits from quick visual cues, but the category section should stay quieter and typographic. Next: if more drawer refinements are needed, keep icon usage limited to the page-link layer rather than extending it into the category list.
- 2026-03-23: Adjusted the mobile drawer page-link underline treatment so the selected underline no longer sits on the text box itself. Why: the text was reading slightly higher than the icons in the same row; moving the underline to an absolute decoration keeps the label vertically centered. Next: if more icon/text alignment polish is needed, keep decorative states off the text metrics where possible.
- 2026-03-23: Refined the homepage category carousel into a more polished card treatment by adding a subtle elevated card shell, an inner rounded image frame, stronger label support over imagery, and lightweight pagination dots synced to the current scroll position. Why: the section needed to feel like intentional cards rather than raw images in a row, while staying responsive and explicitly avoiding any partially visible mobile-card treatment. Next: if the homepage carousel gets more polish later, keep extending this existing component rather than replacing the data flow or introducing a heavier carousel dependency.
- 2026-03-23: Polished the homepage category carousel again by removing the translucent label box so each card label now sits directly on its own stronger bottom gradient, and by restyling the pagination dots with a warm earthy active accent instead of neutral grey. Why: the boxed label treatment was reading like a repeated strip across cards, and the dots needed a more intentional premium feel while staying minimal. Next: if this carousel gets any more visual tuning, keep the label readability local to each card rather than reintroducing shared-looking overlay shapes.
- 2026-03-23: Tightened the homepage category carousel’s visible-card count by switching the cards to explicit responsive slide widths: one strong card view on mobile, two cards across on tablet, and three across on desktop. Why: the section felt too crowded when too many cards were visible at once; fewer visible cards give each category stronger presence and make the homepage feel more curated. Next: if this section is tuned further, keep adjusting the same slide-width model rather than letting the cards expand freely again.
- 2026-03-23: Fixed the homepage category carousel dots so swipe/scroll now updates the active indicator from the real card elements instead of the outer scroll container wrapper, and added a slightly clearer scale transition between active and inactive dots. Why: the first dot implementation was measuring the wrong DOM level, so the active dot could stay stuck while swiping. Next: if the carousel gets more interaction polish later, keep the active-state logic tied to the real slide track rather than duplicating scroll math elsewhere.
- 2026-03-23: Refined the desktop header utility area by turning the language switcher into a compact flag+code pill with a tighter attached dropdown, and by giving the desktop profile/cart icons a matching subtle bordered treatment and more deliberate spacing. Why: the desktop utility cluster felt too weak and incidental compared with the rest of the header; it now reads as one composed premium control group without changing mobile drawer behavior. Next: if desktop header polish continues, keep building from this shared utility treatment rather than introducing separate one-off button styles.
- 2026-03-23: Reverted the desktop header utility controls from the newer pill treatment back to the simpler language/profile/cart presentation. Why: the richer language button and matching icon-button treatment were not desired; the desktop utility area should stay lighter in this version. Next: if desktop header utility polish is revisited later, start from this simpler baseline instead of reusing the reverted pill treatment.
- 2026-03-23: Warmed the shared interface palette in a narrow pass by shifting the global background/text tokens toward ivory and softer charcoal, moving the header/footer shell surfaces and borders onto warmer stone tones, and tuning the homepage carousel controls/dots to the same muted gold-earth accent family. Why: the UI shell felt a bit too generic grey; this keeps the products dominant while giving the interface a more curated handmade-luxury tone. Next: if this palette is extended further, keep pushing shared surfaces and accents from the same warm token family rather than introducing disconnected one-off colors.
- 2026-03-23: Corrected the homepage carousel’s initial active-dot behavior on desktop by tracking the leftmost visible card instead of the viewport center, and removed the category card drop shadows so the cards sit cleaner against the page. Why: centered tracking made the second dot appear active on first load whenever multiple cards were visible, and the card shadows were feeling heavier than needed. Next: if the carousel gets more polish, keep the active-state logic tied to the visible lead card rather than the viewport midpoint.
- 2026-03-23: Added the circular Artiani seal before the `Art and Tech experience by G. Margiani` credit inside the mobile drawer footer. Why: the burger-menu footer needed the same branded signature cue as the main footer instead of showing the text line alone. Next: if the drawer footer is polished further, keep this signature row aligned with the main footer rather than inventing a separate mobile-only credit treatment.
- 2026-03-23: Centered the mobile drawer footer content horizontally so the social links, contact rows, and branded signature now sit on a shared centered axis. Why: the burger-menu footer read unevenly with mixed left-aligned rows after the seal was added. Next: if this block is tuned further, keep the footer stack centered as one compact closing section rather than drifting back into mixed alignments.
- 2026-03-23: Cleaned up the homepage hero and category strip by softening the hero overlay/text shadows, reducing the right-side hero credit size, tightening the category-card border/radius, reintroducing only a very light card shadow, lightening the per-card label treatment, making inactive dots a bit more visible, shrinking and tucking the arrows inward, and opening more vertical space around the carousel block. Why: the homepage discovery area was feeling slightly heavy and crowded; this keeps the current structure while making it read cleaner, calmer, and more premium. Next: if this section gets more polish later, keep working inside the same hero/carousel components rather than expanding the change into broader homepage layout edits.
- 2026-03-23: Tightened homepage typography in a narrow pass by slightly reducing and tightening the desktop nav and brand wordmark spacing, refining the homepage kicker into a lighter more editorial label, constraining and tightening the main homepage heading, and giving the body copy a softer tone with a touch more line-height. Why: the homepage text rhythm needed to feel more curated and calm without changing layout structure or the broader typography system. Next: if homepage type polish continues, keep refining only these local classes rather than introducing new global font rules.
- 2026-03-23: Fixed PDP gallery resolution so variant changes now resolve images from the exact selected `product_images.variant_id` first, preserve all same-variant images in `sort_order`, promote the first `main` image to the hero slot, and fall back to product-level (`variant_id == null`) images only when the selected variant has none. Why: runner materials can intentionally reuse identical storage paths across different variant IDs, so the previous flattened gallery mapping was losing the exact image source-of-truth and could leak sibling-variant images or collapse multi-detail galleries. Next: if order/lookup image selection is tightened later, reuse the same exact-variant gallery helper instead of re-implementing image fallback rules in parallel.
- 2026-03-23: Hardened image selection across pricing and order lookup by reusing the shared exact-variant gallery resolver for thumbnails and adding a tiny pure regression test around shared storage paths, multi-detail ordering, and product-level fallback. Why: these paths still had their own `variant images or all images` shortcut, which could ignore the newer PDP rule that product-level fallback must not pull sibling-variant images. Next: if any other thumbnail path is added later, route it through the same shared resolver instead of introducing another local image picker.
- 2026-03-23: Refined the shared storefront visual system in a controlled pass by introducing warmer global tokens, a serif/display plus UI/body font split, standardized card/radius/button/pill primitives, a cleaner header treatment, quieter carousel dots, and restrained border-based styling across the homepage, catalogue, PDP buy surface, and footer without changing layout structure or business logic. Why: the site needed to feel calmer, more editorial, and less template-like while preserving the existing architecture and content. Next: checkout/track surfaces and the richer media-card fallback visuals still use the older style language and should be normalized in the next visual consistency pass.
- 2026-03-23: Tightened the homepage category carousel card widths again so the strip now shows 2 cards on phone, 3 on tablet, and 4 on wider desktop. Why: after the visual cleanup pass, the homepage category cards still felt a bit oversized and sparse; showing one more card per larger breakpoint makes the strip feel lighter and more curated. Next: if this carousel is tuned again, keep card-count changes limited to the shared slide-width math rather than adding layout-specific overrides elsewhere.
- 2026-03-23: Removed the first homepage category card from the category strip because that same lead category is already featured in the hero banner. Why: repeating the same category immediately below the hero was redundant and made the homepage discovery sequence feel less curated. Next: if homepage category promotion changes later, keep the hero and first carousel slot coordinated rather than surfacing the same lead category twice.
- 2026-03-23: Simplified the homepage further by removing the right-side hero caption block, tightening the hero to only the left eyebrow plus lead category title, switching category-card labels back to plain white text over the image gradient, and temporarily disabling the homepage media rail render. Why: the homepage still felt too component-heavy; this makes the top of the site quieter and more brand-led before the imagery pass. Next: when imagery improves, revisit the homepage media section separately instead of reintroducing it into the current hero/card rhythm.
- 2026-03-24: Added a small shared category-image mapping for the `works` slug so the homepage can use dedicated Paintings imagery without adding a DB-backed category image model yet. The homepage hero now uses the uploaded Paintings desktop/mobile assets through a single mapped source of truth, and Paintings category cards resolve to the mapped desktop image before falling back to lead product imagery. Why: we needed to start using the newly uploaded Paintings visuals immediately while keeping the diff frontend-only and safe. Next: if category images become DB-driven later, replace this temporary slug-based mapping with category records or taxonomy metadata instead of extending the hardcoded map broadly.
- 2026-03-24: Smoothed the homepage hero image overlay gradient by removing the hard top-stop that was reading like a visible seam across the banner. Why: the previous overlay transition created a shadow-like dividing line that made the hero image look split into two sections. Next: if the hero gets more imagery polish later, keep the readability overlay continuous and subtle instead of reintroducing abrupt gradient stops.
- 2026-03-24: Merged short and long table runners back into one storefront display category by mapping both runner subtypes to the same `table_runner` UI filter/group key in the shared catalogue-model layer, and by updating the mobile drawer category link to point at that merged section. Why: the storefront should present runners as one category chip/section/home tile while leaving the underlying DB category and subtype data unchanged. Next: if the catalogue taxonomy UI is revisited later, keep subtype-level distinctions in product data or PDP logic only unless there is a clear merchandising reason to split them again.
- 2026-03-24: Restored the homepage media rail so the YouTube/article/link cards render again beneath the homepage intro block, reusing the existing `getArtistMediaCards()` source instead of changing any homepage structure or content logic. Why: the bottom media cards were intentionally part of the homepage storytelling flow and needed to come back after the earlier cleanup pass removed them. Next: if the homepage gets another calm-down pass later, keep this section present and tune only its styling or card selection rather than removing the entire rail.
- 2026-03-24: Tightened the homepage one last time by reducing the mobile-only hero title size, lifting the mobile text block slightly, adding a stronger mobile-only left darkening gradient for cleaner white-text readability, and making `works` the first homepage category card without changing catalogue ordering. Why: the mobile banner text was still a touch too heavy over the image, and Paintings needed to lead the homepage card strip before the next imagery pass. Next: homepage category order should stay stable now unless a future imagery rollout needs a deliberate editorial sequence across more categories.
- 2026-03-24: Removed the first homepage category from the bottom carousel again so the lead category stays exclusive to the hero and does not repeat immediately below it. Why: the hero already carries the primary category emphasis, so showing the same category as the first card right after it felt redundant. Next: if homepage category sequencing changes again, keep the hero lead and carousel lead coordinated so the first card complements the banner instead of duplicating it.
- 2026-03-24: Added the uploaded Tablecloths category card image to the shared category-image mapping under the stable `tablecloth` slug, so homepage category cards can use it without changing hero logic or product data. Why: Tablecloths needed a dedicated storefront image immediately, and the existing shared mapping layer was already the safest frontend-only source of truth for category visuals. Next: if more category-specific imagery is added, keep extending this same mapping carefully until category images move into taxonomy-backed data.
- 2026-03-24: Added the uploaded Runners category card image to the shared category-image mapping under the merged `table_runner` slug, so the homepage category carousel can use it for the unified runners display bucket without touching product data or hero logic. Why: the storefront now treats long and short runners as one display category, so that merged category needed its own dedicated imagery in the same shared mapping layer. Next: if more merged display categories need dedicated imagery, keep using stable display-category slugs in this mapping until category images move into taxonomy-backed data.
- 2026-03-24: Added the uploaded Pillows category card image to the shared category-image mapping under the stable `pillow` slug, so homepage category cards can use it without changing hero logic or product data. Why: Pillows needed the same dedicated storefront imagery treatment as the other mapped categories, and the shared mapping layer remains the safest frontend-only source of truth for these overrides. Next: if more category card imagery is added, keep the overrides centralized in this mapping until category images become taxonomy-backed.
- 2026-03-24: Added the uploaded Scarves category card image to the shared category-image mapping under the stable `headscarf` slug, so homepage category cards can use it without changing hero logic or product data. Why: Scarves needed the same dedicated storefront imagery treatment as the other mapped categories, and the shared mapping layer remains the safest frontend-only place to centralize these overrides. Next: if more category card imagery is added, keep extending this same mapping carefully until category images become taxonomy-backed.
- 2026-03-24: Switched the runner storage normalization script to copy-first behavior, expanded it to catch the extra `runner-kajar-green-forest-detail.jpeg` typo, and executed it against live Supabase so 15 runner aliases now exist at their canonical DB-referenced names. Why: runner images had been reuploaded under a mix of canonical and typo/legacy filenames, and copy-first normalization is safer than moving or rewriting DB paths. Next: the only runner asset still missing after normalization is `runner-lamb-antique-bordeaux-detail.jpeg`, so reupload that exact file path next.
- 2026-03-24: Rechecked live runner storage after the missing lamb detail upload and verified all DB-referenced runner image paths now resolve, then switched the scarf storage normalization script to copy-first behavior and executed the three live typo-path copies for the couple scarf assets (`anitque`/`liliac` -> canonical names). Why: both runner and scarf uploads were partially restored under mixed filenames, so the safest cleanup was to copy typo objects into the canonical DB paths without deleting originals. Next: continue the same bucket audit for the remaining product families before changing any DB image paths.
- 2026-03-24: Added a one-off circular cloth large-main JPG normalization script, copied the four remaining alias JPG uploads into canonical underscore filenames, switched circular PDP large-main overrides to `.jpg`, and deleted the old canonical circular `.png` large-main assets after the complete JPG set was verified in storage. Why: the new circular large-main uploads were meant to replace the heavier PNG set, but frontend paths and storage names still needed to be aligned before the old PNGs could be safely removed. Next: if rectangular large-main assets are converted to JPG later, repeat the same copy-first path normalization and then switch the rectangular override suffix.
- 2026-03-24: Repeated the same copy-first cleanup for rectangular cloth large-main assets: copied the four alias JPG uploads into canonical underscore filenames, switched rectangular PDP large-main overrides to `.jpg`, and deleted the old canonical rectangular `.png` large-main assets after the complete JPG set was verified in storage. Why: rectangular large-main files had the same mixed naming and heavier PNG carryover as the circular set, so the safest cleanup was to normalize names first and only then remove the old PNGs. Next: the remaining PNG bucket usage is now limited to category/background/home and painting assets, so any further JPG conversion can focus on those groups one by one.
- 2026-03-24: Normalized the newly uploaded runner/scarf category-card JPG files from their old singular/typo storage names into canonical plural JPG names, switched the shared category-image mapping to those JPG paths, removed the old runner/scarf category PNG files from storage, and shortened the Georgian homepage Artiani title by dropping the final word as requested. Why: category cards had finally been reuploaded as lighter JPGs but still needed canonical naming and frontend alignment before the old PNGs could be removed safely. Next: the remaining PNG bucket usage is now limited to backgrounds, paintings, and the homepage pillows image.
- 2026-03-24: Changed the shared header cart count badge from the dark neutral fill to a red fill so the basket state reads more clearly at a glance without altering layout or cart behavior. Why: the header needed a stronger visual accent for the cart count while keeping the rest of the navigation unchanged. Next: if header accents are tuned again, keep them consistent with the museum-minimal palette and avoid spreading the same red treatment to unrelated nav elements.
- 2026-03-24: Tightened the order-create API payload validation by adding a server-side email format check alongside the existing phone and delivery validation, without changing the checkout UI flow or error shape. Why: browser `type="email"` is not enough on its own, and launch traffic should not be able to persist obviously invalid customer emails into orders. Next: continue the pre-launch hardening by splitting the catalogue product queries so PDPs stop loading the full catalogue for single-product and related-product work.
- 2026-03-24: Split the catalogue query path so PDPs now fetch the requested product by slug and a small related-product set directly, instead of loading the full active catalogue in memory for every product page request. Why: the previous PDP path scaled poorly because both metadata and page rendering depended on full-catalogue fetches just to find one product and render four related cards. Next: remove the remaining dynamic/random homepage and PDP rendering patterns so more of the storefront can benefit from stable caching.
- 2026-03-24: Removed the homepage `force-dynamic` override in favor of a stable one-hour revalidation window and stopped shuffling homepage media cards server-side, so homepage output is now deterministic and more cache-friendly while keeping the same content set and DB-driven ordering. Why: server-side randomness and forced dynamic rendering were undermining cacheability and making render output unnecessarily unstable before launch. Next: if homepage freshness needs tighter control later, tune the revalidation window explicitly rather than reintroducing random or always-dynamic rendering.
- 2026-03-24: Added a first-layer IP-based in-memory rate limiter for the two public sensitive order routes: `orders/create` is now limited to 5 requests per 10 minutes per IP, and `orders/lookup` is limited to 10 requests per 10 minutes per IP, both with generic `429` responses and `Retry-After` headers. Why: launch traffic needed basic abuse protection against order spam and lookup brute force without introducing a new external service on the eve of go-live. Next: if production traffic grows or multiple server instances become common, move this limiter to a shared backend such as Upstash Redis so the limits are enforced globally instead of per-process.
- 2026-03-24: Updated the final sentence of the Georgian About Artiani text to the newly approved closing line in the shared dictionary. Why: the about-page ending needed the final language adjustment before launch, and the change belongs in i18n source rather than page markup. Next: keep any remaining last-minute copy edits centralized in the dictionary files so EN/KA page structure stays untouched.
- 2026-03-24: Added a narrow cart-title display helper and applied it across the cart page, PDP basket preview, and checkout summaries so Georgian large-runner basket entries no longer leak the English word `Large` in visible item titles. Why: this was a display-only wording issue in shared basket surfaces, not a product-data or slug problem, so the safest fix was to normalize the visible title at render time only for Georgian large-runner slugs. Next: if more localized product-title cleanup is needed later, keep it at the basket/display layer unless the underlying translations themselves are being corrected.
- 2026-03-24: Made the PDP mobile color swatch strip horizontally scrollable inside the gallery overlay, so products with many color/background options no longer clip the later swatches off-screen on phones. Why: the image-overlay picker was laid out in a single non-wrapping row with a max width, which meant part of the picker could become unreachable on smaller screens. Next: if PDP mobile interaction is refined further, keep the swatch rail compact and scrollable rather than wrapping it into multiple rows over the image.
- 2026-03-24: Tightened the mobile catalogue card rhythm so product titles fit more reliably, subtitles can use two lines on phones before collapsing back to one line on larger screens, and the add-to-cart button now sits in the card footer on mobile instead of floating over the image/text area. Why: the previous mobile card layout could clip subtitle text and let the basket button visually compete with the title area. Next: if catalogue mobile density is tuned again, keep the footer action anchored low in the card and adjust only text measure or spacing before changing the card structure.
- 2026-03-24: Tightened the mobile burger-drawer spacing between the main catalogue link group and the category link list by trimming the primary block bottom padding and slightly reducing the category row height/gap. Why: the previous separation made `Catalogue` and the category list feel visually too far apart in the drawer. Next: if the drawer gets another polish pass, keep the information hierarchy but tune spacing before changing link order or iconography.
- 2026-03-24: Extended the Georgian large-runner basket cleanup to the visible product-type overline as well, and applied the same title normalization to track-order item titles for safety. Why: the remaining `Large მაგიდის რანერი` leak was coming from shared basket labels, not only from the main item title. Next: if any more large-runner wording leaks appear, keep normalizing them at the shared display helper layer instead of changing slugs or stored product data.
- 2026-03-24: Normalized the Georgian Good Shepherd wording from `კეთილი მწყემსი` to `მწყემსი კეთილი` in live Supabase and added a migration that applies the same replacement in the main KA translation tables for other environments. Why: the request was to eliminate the older phrasing everywhere it still appeared, and the remaining occurrence was in live product translation data rather than frontend copy. Next: if more naming standardization requests come in, keep scanning both repo strings and live translation tables before deciding whether the fix belongs in code, data, or both.
- 2026-03-24: Added a washable-care note directly inside the non-painting PDP material section, moved the mobile PDP color picker into its own horizontal scroll rail below the image, and tightened the remaining gap between the burger drawer’s `Catalogue` block and the category list. Why: the material panel needed one more practical product fact, the mobile color picker still needed to stay fully visible without covering the image, and the mobile menu separation was still reading too loose. Next: if PDP mobile polish continues, keep the swatch rail outside the image on phones and adjust its density before changing the gallery structure again.
- 2026-03-24: Changed shared cart insertion to report whether it actually changed the cart, and wired catalogue/PDP add-to-cart feedback to show the temporary `Added` state only on real cart changes. Why: painting products are intentionally single-quantity, so clicking their add button again should not falsely claim the item was added when the cart entry was simply left unchanged. Next: if more product types get quantity caps later, keep the no-op/add distinction at the shared cart API boundary so all button feedback stays honest automatically.
- 2026-03-24: Constrained the mobile PDP color rail and thumbnail rail to the gallery width so their horizontal scrolling no longer makes the entire product page scroll sideways. Why: the separate mobile scroll rows were allowed to size to their contents, which caused page-level horizontal overflow on phones. Next: if the mobile gallery gets more polish, keep every horizontal scroller explicitly width-constrained so only the intended rail scrolls.
- 2026-03-24: Added `min-w-0` and horizontal overflow containment to the PDP section/grid columns so the product details page itself can no longer widen when nested mobile gallery content grows. Why: the mobile gallery rails were not the only contributor; the surrounding PDP layout also needed explicit width containment to prevent page-level sideways scrolling. Next: if PDP mobile layout changes again, keep the top-level section and both grid columns explicitly width-constrained before debugging child scrollers.
