# NOTES

- 2026-03-16: Updated the V2 catalogue/product UI copy so `tablecloth_square` is presented as Rectangular everywhere user-facing while leaving the DB enum unchanged. Why: the UI taxonomy should read Cloth / Round / Rectangular rather than exposing the old square wording. Next: keep DB-backed taxonomy mapping centralized in the UI layer as more product types are added.
- 2026-03-16: Built the first real V2 Supabase-backed catalogue and product detail flow for `tablecloth_round` and `tablecloth_square`, including locale fallback (`requested -> en -> ka`), filter pills, card grid, and a simple `/[lang]/product/[slug]` page. Why: replace the placeholder catalogue with real DB-backed reads while keeping orders, tracking, env, and mail untouched. Next: add variant selection state, richer product copy/media handling, and later connect cart/order flows to the same product source of truth.
- 2026-03-16: Swapped the mobile shell nav from a hidden menu back to always-visible page buttons, arranged as a compact multi-line grid below the top row. Why: the menu hid too much navigation context, but horizontal scrolling also felt weak on very small screens. Next: keep this visible mobile nav until real page content determines a stronger information architecture.
- 2026-03-16: Replaced the mobile shell’s horizontally scrollable nav pills with a compact menu toggle and dropdown panel while keeping the compact top row and existing desktop nav. Why: the scrollable nav could hide page buttons on very small screens and felt fragile. Next: keep this mobile menu pattern while real content is rebuilt inside the shell.
- 2026-03-16: Tightened the V2 shell mobile UX by making the shared header compact and sticky on small screens, moving language buttons into the top row, switching mobile page navigation to a single-line horizontally scrollable pill row, and reducing placeholder top spacing so headings sit higher. Why: the shell header was too tall and the nav wrapped awkwardly on phones. Next: keep this compact shell while rebuilding real page content and product flows on top of it.
- 2026-03-16: Reset the Next.js storefront to a minimal V2 shell with only locale-aware placeholder pages for `/[lang]`, `/biography`, `/catalogue`, `/cart`, and `/track`; added `ru` dictionaries; replaced deprecated `middleware.ts` with `proxy.ts`; and removed the old storefront UI/routes/data from active usage while preserving env, Supabase, and mail helper infrastructure. Why: establish a clean rebuild foundation without exposing secrets or breaking deployability. Next: define the new domain model and reintroduce catalogue, product, cart, and order flows incrementally on top of this shell.
- 2026-03-16: Added a Supabase-backed catalogue/product read path for `tablecloth_round` and `tablecloth_square` without removing the old hardcoded product path. Why: start the storefront migration to database-backed product reads while keeping checkout/order/tracking logic untouched and preserving a fallback for non-migrated slugs. Next: add anon/public Supabase envs, migrate cart/order pricing to DB-backed variants, and expand the same query layer to the homepage and remaining product families.
- 2026-03-16: Added `PROJECT_AUDIT.md`, `ARCHITECTURE_MAP.md`, and `RUNNING_THE_PROJECT.md` after a read-first repository audit. Why: provide a concrete engineering handoff covering stack, structure, runtime flows, setup, risks, and next steps without changing application code. Next: add `.env.example`, document Supabase schema/migrations, and replace deprecated `middleware.ts` with the current Next.js `proxy` convention.
- 2026-02-24: Initialized Phase 0. Updated AGENTS.md and created repo skills + audit checklist in progress.
- 2026-02-24: Added repo skills under .agents/skills and refreshed AGENTS.md per new workflow.
- 2026-02-25: Removed OTP/Twilio verification artifacts (deleted OTP/lookup/bid API routes, removed track page/component/lib, removed OTP auction + track i18n keys, and removed Twilio env notes from AGENTS.md), and fixed codex-workloop SKILL frontmatter YAML quoting. Why: roll back SMS verification and restore pre-OTP behavior. Next: if tracking/bid verification is needed later, reintroduce with a non-Twilio flow behind a dedicated feature branch.

## Phase 1 Audit (sequential — sub-agents not available)

### Prioritized checklist
1) i18n-sweep: move remaining user-facing literals into EN/KA dictionaries
- Why: Requirement: no hardcoded UI copy.
- Files: web/app/[lang]/layout.tsx, web/app/[lang]/product/[slug]/page.tsx, web/src/components/CheckoutForm.tsx, web/src/components/ProductDetails.tsx, web/src/components/CartView.tsx, web/src/i18n/en.json, web/src/i18n/ka.json.
- Risk: Low (string-only changes).
- Test/Verify steps: Run `npm run lint` and `npm run build`. Check `/ka/product/*` and `/en/product/*` titles, checkout confirmation text, cart summary lines.

2) Remove unused i18n keys + stale “campaign” copy where still referenced
- Why: Reduce leftovers / context bloat; align copy with current museum-minimal direction.
- Files: web/src/i18n/en.json, web/src/i18n/ka.json; confirm usage in web/src/components/ProductDetails.tsx and cart/checkout.
- Risk: Low (copy cleanup).
- Test/Verify steps: Lint/build; spot-check product details empty-options text and cart empty subtitle.

3) Cleanup unused imports / minor leftovers
- Why: Reduce noise and keep diffs clean.
- Files: web/src/components/ShopCatalog.tsx (unused import), any unused helpers found by lint.
- Risk: Low.
- Test/Verify steps: `npm run lint` clean.

4) UI component dedupe (optional, small)
- Why: Reduce repeated button/chip markup without changing layout.
- Files: web/src/components/*, possibly add `web/src/components/ui/*`.
- Risk: Medium (could shift layout if careless).
- Test/Verify steps: Lint/build; visually compare buttons/chips across Home, Product, Cart, Checkout.

5) Next.js sanity pass
- Why: Catch App Router pitfalls (metadata, async params, Link scroll).
- Files: web/app/[lang]/*, web/src/components/ProductDetails.tsx, web/src/components/ShopCatalog.tsx.
- Risk: Low to medium.
- Test/Verify steps: Run dev server, open `/ka/catalogue`, `/ka/product/[slug]`, `/en/product/[slug]` and verify scroll + titles.
- 2026-02-24: Item #1 i18n-sweep: moved remaining UI literals to EN/KA (site title, separators, qty labels, bank fields) and wired usages. Verify: `npm run lint`; `npm run build` (fails to fetch Google fonts offline).
- 2026-02-24: Item #2 cleanup: removed unused i18n keys (home.hero.kicker, shop.kicker/subtitle/card_view, product.personalization_subtitle/campaign_price) and cleaned “campaign” copy. Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Item #3 cleanup: fixed ShopCatalog missing useState import (runtime error) and kept imports tidy. Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Item #4 ui-components: added reusable Chip and applied to catalogue filters + cards toggles; suppressed cart count hydration mismatch. Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Item #5 nextjs-sanity: fixed type cast in ShopCatalog filter labels (kind vs type). Verify: `npm run lint`; `npm run build` fails offline Google Fonts.
- 2026-02-24: Disabled Google Fonts fetch by removing next/font usage and setting serif font stack in globals. Verify: `npm run lint`; `npm run build` fails in sandbox (Turbopack PostCSS process permission).
- 2026-02-24: Fixed build type error by widening layout params to string and normalizing via isLocale/defaultLocale. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Fixed cart count hydration by loading localStorage on mount (CartProvider) and keeping save guard. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Adjusted cart hydration to use useLayoutEffect so header count updates before paint. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Updated header layout (nav links next to brand + active state) and enlarged PDP back button. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Added underline active state for header nav links. Verify: `npm run lint`, `npm run build`.
- 2026-02-24: Centered brand and stacked nav under it on mobile in SiteNav. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Removed the per-item "each" suffix from cart line-item prices so rows show only the price value. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Fixed cart reset on language switch by skipping the first post-hydration cart persistence write in CartProvider. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added checkout-style phone country picker to painting auction bid form (country code + local number input). Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added minimal server-only infra for Supabase + transactional email (`env.server.ts` validation and memoized `getSupabaseAdmin`) with required env checks including `ORDERS_ADMIN_EMAIL`. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added server-side order pricing recomputation (`orderPricing.ts`) and Node-crypto order code generation with unique-constraint retry helper (`orderCode.ts`) for future order API use. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Implemented checkout order creation flow: Node runtime API route inserts `orders` + `order_items` in Supabase with server-side repricing and unique code generation, sends customer/admin transactional emails, and CheckoutForm now submits to API with success/error states. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added order tracking by code+email: Node runtime lookup API (`/api/orders/lookup`) and new `/[lang]/track` page with localized form/results (status, created date, items, total) and generic not-found handling. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added localized track-order link to header navigation (`SiteNav`) with the same nav link styling and locale-preserving route. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Split server env config into required `envSupabase` and optional `envEmail`; order creation now proceeds without email env and returns `emailSent: false` when email config is missing. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Updated tracking UX: `track.linkLabel` now reads “Track order” and order lookup now returns all orders for an email once a valid `code+email` pair is provided, with track page rendering all matched orders. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Switched transactional email transport to Gmail SMTP via Nodemailer; env now uses optional `envMail` (`GMAIL_USER`, `GMAIL_APP_PASSWORD`, `ORDERS_FROM_EMAIL`, `ORDERS_ADMIN_EMAIL`) while order creation remains successful when email sending is unavailable. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Localized order email templates by order language, fixed email track links to absolute `${PUBLIC_BASE_URL}/${lang}/track` (fallback `http://localhost:3000`), and added reusable localized payment instruction blocks (TBC/BOG IBAN + transfer reference) via `paymentInstructions.ts`. Why: ensure checkout emails are language-correct and include actionable tracking/payment details. Next: replace placeholder IBAN/account values with final production banking details. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Enhanced order email line-item details to include product kind and selected options (signed/custom text/postcard or greeting card back), so customers/admin can see exactly what was ordered in each line. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Added localized bank transfer block to checkout success state (amount, reference/order code, account name, TBC + BOG IBANs, and transfer notes) directly below order confirmation code. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Implemented real auction bid flow: added Node API route `/api/bids/create` with validation, Supabase persistence in `bids`, BID code generation with unique retry, and non-blocking bidder/admin SMTP emails (`emailBids.ts`) including localized copy, absolute track link, and auction deposit instructions via `paymentInstructions` variant. Updated painting bid form to submit to API (required email field), show bid success code/email status, and keep localized generic error handling. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Changed painting bid UX to redirect after successful submit to dedicated localized result page (`/[lang]/bid`) instead of rendering inline; page shows bid code, email status, deposit transfer block (reference + TBC/BOG IBANs), and tracking link. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Fixed bid-result translation flow by preserving query params during language switch in `SiteNav` (so bid code/details survive locale toggle), and added painting details on bid result page (painting title/slug + bid amount from submit context). Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Expanded tracking lookup so a valid `email + one code` match can be either order or bid code; API now returns both all orders and all auction bids for that email, and track UI renders both sections. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Disabled interactive form fields during submission across checkout, tracking lookup, and painting bid forms (inputs/selects/textareas/buttons) to prevent duplicate submits and in-flight edits. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Fixed hydration mismatch from currency formatting by replacing `Intl.NumberFormat` in `money.ts` with deterministic server/client-safe grouping logic, so SSR and browser render identical GEL strings. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Hardened bid amount handling for large values: precise cents parsing (up to 2 decimals), explicit upper bound validation, and out-of-range DB insert errors now return 400 (validation) instead of 500. Bid input now includes `step=0.01` and a max aligned with cents bound. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Updated auction bid range to enforce `minimum bid ... 2,000,111` GEL consistently in both client input (`max`) and backend validation. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Refactored Home into a new server-rendered `HomePage` component with a paintings-first, product-led layout: hero with featured painting + thumbnails, featured originals grid, gifts category tiles, curated bundle collages, compact museum-label bio, and final CTA strip. Home now uses product images from `products.ts`, shows no prices, and removes the previous instructions block. Added new localized `home.*` keys in EN/KA with parity. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home follow-up polish: reduced hero painting visual size, added a visible hero auction badge, updated hero subtitle copy to begin with paintings emphasis, moved painting grid lower so section 2 is non-painting, and made gifts/bundle tiles fully clickable (including category-filter links like `catalogue?type=calendars`). Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home follow-up cleanup: removed the bottom “Featured originals” block and moved the compact Levan bio block into the hero area directly under CTA buttons to use top-section whitespace better. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home microcopy/UI tweak: removed per-card “დათვალიერება/Explore” labels from gifts and bundle tiles and replaced with subtle `>` affordance while keeping the whole tile clickable. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Home finalization polish: kept catalogue `?type=` links (confirmed ShopCatalog query filtering path works), removed static hero bid-count text while keeping AUCTION badges, and adjusted hero spacing/details per latest UX feedback. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Redesigned product detail UX with a split component architecture: interactive gallery with lightbox (`ProductGallery` + `ProductLightbox`), sticky purchase panel (`ProductPurchasePanel`) preserving existing cart/auction logic, structured Story/Details/Delivery info sections (`ProductInfoSections`), and server-rendered related items grid (`RelatedProducts`). Added required localized product UI keys (EN/KA parity) and kept layout museum-minimal with desktop two-column flow and mobile sticky action bar. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: UI polish pass for mobile nav + product detail UX: rebuilt `SiteNav` mobile behavior with compact header (logo left, cart + menu right) and locale-preserving drawer links (Home/Catalogue/Cart/Track/About + language toggle), tightened product gallery thumbnails with subtler selected state, reduced hero artwork padding, and softened purchase/auction panel visual weight while keeping sticky mobile CTA behavior and existing cart/bid flows unchanged. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Removed custom text personalization (`addText`) across product, cart, checkout, pricing, and order email flows; only signature and card back variant options remain active. Updated cart item ID/options shape and removed unused i18n keys related to custom text. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Removed the Product Details "Story" info card and reflowed info layout to Details + Delivery only, constrained to the left content column (`lg:col-start-1`) so these cards no longer span beneath/intersect the sticky purchase panel column. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Simplified product-page prev/next navigation: removed the large slab card block, added subtle previous/next arrow buttons over the hero gallery image, and added a small editorial "next work" link below related works. Navigation now prefers same-kind sequence and falls back to global `products.ts` order at edges. Verify: `npm run lint`, `npm run build`.
- 2026-02-25: Replaced placeholder painting catalogue entries with three real auction listings (`shoba2`, `shoba`, `angelozi`) in `products.ts`, including localized names/summary/description and Supabase Storage image URLs with auction metadata (min bid + end date). Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Replaced placeholder cards listings with real `memories-cards` data in `products.ts` (localized copy + Supabase postcard/greeting images), kept card personalization without `addText` (signature remains), and set pricing so greeting back adds +10 GEL on top of postcard base while signature surcharge still applies. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Updated painting image rendering to avoid cropping in product/gallery and homepage contexts (contain fit with paper-tone frames), adjusted painting prev/next nav thumbs to avoid crop, and restyled AUCTION badges to high-contrast top-left pills with shadow for better readability in detail and catalogue/home cards. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added 4 real bookmark products (bundle + 3 singles) with Supabase images. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added 3 real calendar products (bundle + wall + desk) with Supabase images. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added print size variants support (UI + server pricing). Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Home update: surfaced a compact above-the-fold quick-shop row (cards/bookmarks/calendars product tiles) under hero CTAs and removed the Curated Sets/Bundles section while keeping paintings featured. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Added 3 posters + 1 poster bundle with size variants. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Fixed metadata description i18n key mismatch in `app/[lang]/layout.tsx` by switching from missing `home.subtitle` to existing `home.hero.subtitle`, preventing raw key fallback in page metadata. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Updated Levan Margiani biography content in EN/KA across About page sections (overview, practice, selected milestones) and Home bio snippet with link to About, keeping museum-minimal tone and bilingual parity. Verify: `npm run lint`, `npm run build`.
- 2026-02-28: Made Home quick-shop tiles mobile-safe by using responsive grid columns (1/2/3 across breakpoints) and preventing tile min-width overflow, removing horizontal page scroll caused by the quick tiles section. Verify: `npm run lint`, `npm run build`.
